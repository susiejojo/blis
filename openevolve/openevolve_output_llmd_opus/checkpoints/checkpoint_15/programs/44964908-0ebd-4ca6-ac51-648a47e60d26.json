{"id": "44964908-0ebd-4ca6-ac51-648a47e60d26", "code": "\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load balancing\n        best_pods = []\n        best_score = -1\n        pod_loads = [0] * num_sims\n        \n        # Count current load per pod\n        for p in policy:\n            pod_loads[p] += 1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            consecutive = 0\n            for i, key in enumerate(block_keys):\n                if key in pod_cache:\n                    consecutive += 1\n                    score += 1\n                else:\n                    break\n            \n            # Bonus for any additional matches beyond prefix\n            if consecutive < len(block_keys):\n                for key in block_keys[consecutive:]:\n                    if key in pod_cache:\n                        score += 0.1  # Small bonus for non-consecutive matches\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Tie-breaking: choose least loaded pod\n        if best_pods:\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n        else:\n            best_pod = 0\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router", "language": "python", "parent_id": "f8ede7b5-e9ad-4bdb-a512-cedb095fef98", "generation": 1, "timestamp": 1770389771.011109, "iteration_found": 6, "metrics": {"combined_score": -31216.944914691943}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 14 lines with 28 lines\nChange 2: Replace # ================== with 15 lines\nChange 3: Replace 4 lines with 4 lines\nChange 4: Replace 7 lines with 15 lines", "parent_metrics": {"combined_score": -11081.048222748814}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYou are optimizing a Python request router for large language model inference.\nThe router assigns each request to one of N pods by:\nTokenizing the prompt\nHashing tokens into KV blocks\nScoring pods by longest prefix match with their KV cache\nRouting to the pod with the highest score\nUpdating that pods KV cache after routing If no prefix match exists, a round-robin fallback is used.\nThe router is evaluated in a simulator that measures end-to-end latency (average and tail).\nGoal: The goal is to maximize the average reward returned by the simulators. You May\nModify the prefix scoring logic\nChange tie-breaking behavior\nAdd lightweight load-aware or probabilistic decisions\nIntroduce simple thresholds or heuristics\n", "user": "# Current Program Information\n- Fitness: -11081.0482\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at -11081.0482\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: combined_score: -11081.0482\n- Outcome: Regression in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-11081.0482)\n\n\n\n# Current Program\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Looking at the current program, I see several opportunities to improve the fitness score by optimizing the routing logic. The main areas for improvement are:\n\n1. **Load balancing**: Currently, the router doesn't consider pod load, which can lead to uneven distribution\n2. **Tie-breaking**: When multiple pods have the same score, we always pick the first one\n3. **Cache efficiency**: We could improve how we handle partial matches\n\nHere are my targeted improvements:\n\n## 1. Add load-aware routing with tie-breaking\n\n<<<<<<< SEARCH\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n=======\n        # 3. Longest-prefix scoring with load balancing\n        best_pods = []\n        best_score = -1\n        pod_loads = [0] * num_sims\n        \n        # Count current load per pod\n        for p in policy:\n            pod_loads[p] += 1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Tie-breaking: choose least loaded pod\n        if best_pods:\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n        else:\n            best_pod = 0\n>>>>>>> REPLACE\n\n## 2. Improve cold-start distribution\n\n<<<<<<< SEARCH\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n=======\n        # =========================\n        # Cold-start fairness with load awareness\n        # =========================\n        if best_score == 0:\n            # Choose least loaded pod for better distribution\n            pod_loads = [0] * num_sims\n            for p in policy:\n                pod_loads[p] += 1\n            best_pod = pod_loads.index(min(pod_loads))\n>>>>>>> REPLACE\n\n## 3. Add partial match bonus for better cache utilization\n\n<<<<<<< SEARCH\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load balancing\n=======\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load balancing\n>>>>>>> REPLACE\n\nWait, let me revise this to add the partial match scoring:\n\n<<<<<<< SEARCH\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n=======\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            consecutive = 0\n            for i, key in enumerate(block_keys):\n                if key in pod_cache:\n                    consecutive += 1\n                    score += 1\n                else:\n                    break\n            \n            # Bonus for any additional matches beyond prefix\n            if consecutive < len(block_keys):\n                for key in block_keys[consecutive:]:\n                    if key in pod_cache:\n                        score += 0.1  # Small bonus for non-consecutive matches\n>>>>>>> REPLACE\n\n## Reasoning\n\nThese changes improve the router in several ways:\n\n1. **Load-aware tie-breaking**: When multiple pods have the same prefix match score, we now choose the least loaded pod. This helps distribute requests more evenly and reduces tail latency.\n\n2. **Better cold-start distribution**: Instead of simple round-robin, we actively track load and assign new requests to the least loaded pod. This prevents hot spots during initialization.\n\n3. **Partial match scoring**: We give a small bonus (0.1) for non-consecutive block matches. This helps utilize pods that have some relevant cached data even if it's not a perfect prefix match.\n\nThese optimizations should improve both average and tail latency by:\n- Reducing queue buildup on individual pods\n- Better utilizing available cache across all pods\n- Maintaining good prefix locality while avoiding extreme load imbalance\n\nThe changes are lightweight and don't significantly increase complexity, maintaining the core prefix-matching algorithm while adding smart load balancing."]}}, "artifacts_json": "{\"avg_latency\": 31216.944914691943}", "artifact_dir": null, "embedding": null}