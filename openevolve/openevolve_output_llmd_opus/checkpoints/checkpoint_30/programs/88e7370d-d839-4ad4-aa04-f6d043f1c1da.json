{"id": "88e7370d-d839-4ad4-aa04-f6d043f1c1da", "code": "\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n    pod_request_count = [0] * num_sims  # Track requests per pod\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie-breaking\n        best_pods = []\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0.0\n            consecutive_hits = 0\n            # Use gentler decay and reward consecutive matches\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Gentler decay: 1.0, 0.95, 0.9025, ...\n                    # Plus bonus for consecutive matches\n                    score += (0.95 ** idx) * (1 + 0.1 * consecutive_hits)\n                    consecutive_hits += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) < 0.001:  # Float comparison tolerance\n                best_pods.append(pod_id)\n        \n        # Advanced tie-breaking considering both load and cache utilization\n        if len(best_pods) > 1:\n            # Consider both request count and cache size\n            def pod_load_score(p):\n                cache_penalty = len(POD_KV_CACHE[p]) / 1000.0  # Normalize cache size\n                return pod_request_count[p] + cache_penalty * 0.3\n            best_pod = min(best_pods, key=pod_load_score)\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n        pod_request_count[best_pod] += 1  # Track pod usage\n\n        # 5. Update KV cache with size management\n        POD_KV_CACHE[best_pod].update(block_keys)\n        \n        # Implement cache eviction if needed\n        max_cache_size = 3000  # Per-pod limit\n        if len(POD_KV_CACHE[best_pod]) > max_cache_size:\n            # Keep most recent 80% of cache\n            cache_list = list(POD_KV_CACHE[best_pod])\n            keep_size = int(max_cache_size * 0.8)\n            POD_KV_CACHE[best_pod] = set(cache_list[-keep_size:])\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router", "language": "python", "parent_id": "956a061d-0f85-4fdb-9bd9-03bdfba2fff7", "generation": 3, "timestamp": 1770390074.107889, "iteration_found": 29, "metrics": {"combined_score": -11081.048222748814}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 14 lines with 17 lines\nChange 2: Replace 6 lines with 9 lines\nChange 3: Replace 2 lines with 10 lines\nChange 4: Replace # ================== with 20 lines", "parent_metrics": {"combined_score": -11081.048222748814}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYou are optimizing a Python request router for large language model inference.\nThe router assigns each request to one of N pods by:\nTokenizing the prompt\nHashing tokens into KV blocks\nScoring pods by longest prefix match with their KV cache\nRouting to the pod with the highest score\nUpdating that pods KV cache after routing If no prefix match exists, a round-robin fallback is used.\nThe router is evaluated in a simulator that measures end-to-end latency (average and tail).\nGoal: The goal is to maximize the average reward returned by the simulators. You May\nModify the prefix scoring logic\nChange tie-breaking behavior\nAdd lightweight load-aware or probabilistic decisions\nIntroduce simple thresholds or heuristics\n", "user": "# Current Program Information\n- Fitness: -11081.0482\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at -11081.0482\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### avg_latency\n```\n11081.048222748814\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 10 lines with 11 lines\nChange 2: Replace 5 lines with 6 lines\nChange 3: Replace 5 lines with 5 lines\nChange 4: Replace 3 lines with 11 lines\nChange 5: Replace 2 lines with 3 lines\nChange 6: Replace 5 lines with 7 lines\n- Metrics: combined_score: -11081.0482\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace 14 lines with 25 lines\nChange 2: Replace # ================== with 15 lines\nChange 3: Replace 2 lines with 14 lines\n- Metrics: combined_score: -11081.0482\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace 14 lines with 32 lines\nChange 2: Replace 5 lines with 17 lines\nChange 3: Replace # ================== with 19 lines\n- Metrics: combined_score: -11081.0482\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with weighted blocks and tie-breaking\n        best_pods = []\n        best_score = -1\n        pod_scores = {}\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            # Weight earlier blocks more heavily\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Exponential decay: 1.0, 0.9, 0.81, ...\n                    score += 0.9 ** idx\n                else:\n                    break\n            pod_scores[pod_id] = score\n            \n            if score > best_score + 0.001:  # Small epsilon for float comparison\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) <= 0.001:\n                best_pods.append(pod_id)\n        \n        # Choose among ties based on current load\n        if len(best_pods) == 1:\n            best_pod = best_pods[0]\n        else:\n            # Count current assignments per pod\n            pod_loads = [0] * num_sims\n            for p in policy:\n                pod_loads[p] += 1\n            # Select least loaded pod among best candidates\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache with adaptive limit\n        # Add all blocks but manage total cache size\n        POD_KV_CACHE[best_pod].update(block_keys)\n        \n        # Implement cache eviction if needed (keep most recent)\n        max_cache_size = 5000  # Per-pod limit\n        if len(POD_KV_CACHE[best_pod]) > max_cache_size:\n            # Keep only the most recent entries\n            recent_blocks = set()\n            for prev_idx in range(max(0, len(policy) - 100), len(policy)):\n                if prev_idx < len(policy) and policy[prev_idx] == best_pod:\n                    # This is approximate - in practice would track actual blocks\n                    pass\n            # For now, just clear oldest entries by keeping 80% of cache\n            keep_size = int(max_cache_size * 0.8)\n            if len(POD_KV_CACHE[best_pod]) > keep_size:\n                POD_KV_CACHE[best_pod] = set(list(POD_KV_CACHE[best_pod])[-keep_size:])\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-11081.0482)\n\n### Program 2 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load-aware tie-breaking\n        best_pods = []\n        best_score = -1\n        \n        # Track pod loads for tie-breaking\n        pod_loads = [0] * num_sims\n        for p in policy:\n            pod_loads[p] += 1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Choose least loaded pod among best candidates\n        best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route with exploration for partial matches\n        # If we have a partial match and multiple pods are underutilized,\n        # occasionally explore to spread cache\n        if best_score > 0 and best_score < len(block_keys) * 0.5:\n            pod_utilization = [len(POD_KV_CACHE[p]) for p in range(num_sims)]\n            min_util = min(pod_utilization)\n            if pod_utilization[best_pod] > min_util * 1.5:\n                # Find underutilized pods\n                underutilized = [p for p in range(num_sims) \n                               if pod_utilization[p] < min_util * 1.2]\n                if underutilized and len(policy) % 10 == 0:  # 10% exploration\n                    best_pod = underutilized[len(policy) % len(underutilized)]\n        \n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-11081.0482)\n\n### Program 3 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache and track pod loads\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n    \n    pod_loads = [0] * num_sims\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie tracking\n        best_pods = []\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Exponentially decay weight for later blocks\n                    score += 1.0 / (1.0 + idx * 0.1)\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Choose least loaded pod among ties\n        if len(best_pods) == 1:\n            best_pod = best_pods[0]\n        else:\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route and update load\n        policy.append(best_pod)\n        pod_loads[best_pod] += 1\n\n        # 5. Update KV cache with adaptive limit\n        # Allow more blocks for longer requests\n        new_blocks = [k for k in block_keys if k not in POD_KV_CACHE[best_pod]]\n        if new_blocks:\n            # Adaptive limit: at least 5, at most 20, based on request length\n            limit = min(20, max(5, len(block_keys) // 2))\n            POD_KV_CACHE[best_pod].update(new_blocks[:limit])\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-11081.0482)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {i: set() for i in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Alternative approach to combined_score\n\n### Program D2 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n    pod_request_count = [0] * num_sims  # Track requests per pod\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie-breaking\n        best_pods = []\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0.0\n            # Weight early blocks more heavily for better cache locality\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Exponential decay: first block worth 1.0, second 0.8, third 0.64, etc\n                    score += 0.8 ** idx\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Tie-breaking: choose least loaded pod among ties\n        if len(best_pods) > 1:\n            # Select pod with minimum requests so far\n            best_pod = min(best_pods, key=lambda p: pod_request_count[p])\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n        pod_request_count[best_pod] += 1  # Track pod usage\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -11081.0482, Type: Exploratory)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nUnique approach: [Fragment formatting error: 'metric_name'], Comprehensive implementation\n\n### Inspiration 2 (Score: -11081.0482, Type: Exploratory)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with weighted blocks and tie-breaking\n        best_pods = []\n        best_score = -1\n        pod_scores = {}\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            # Weight earlier blocks more heavily\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Exponential decay: 1.0, 0.9, 0.81, ...\n                    score += 0.9 ** idx\n                else:\n                    break\n            pod_scores[pod_id] = score\n            \n            if score > best_score + 0.001:  # Small epsilon for float comparison\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) <= 0.001:\n                best_pods.append(pod_id)\n        \n        # Choose among ties based on current load\n        if len(best_pods) == 1:\n            best_pod = best_pods[0]\n        else:\n            # Count current assignments per pod\n            pod_loads = [0] * num_sims\n            for p in policy:\n                pod_loads[p] += 1\n            # Select least loaded pod among best candidates\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache with adaptive limit\n        # Add all blocks but manage total cache size\n        POD_KV_CACHE[best_pod].update(block_keys)\n        \n        # Implement cache eviction if needed (keep most recent)\n        max_cache_size = 5000  # Per-pod limit\n        if len(POD_KV_CACHE[best_pod]) > max_cache_size:\n            # Keep only the most recent entries\n            recent_blocks = set()\n            for prev_idx in range(max(0, len(policy) - 100), len(policy)):\n                if prev_idx < len(policy) and policy[prev_idx] == best_pod:\n                    # This is approximate - in practice would track actual blocks\n                    pass\n            # For now, just clear oldest entries by keeping 80% of cache\n            keep_size = int(max_cache_size * 0.8)\n            if len(POD_KV_CACHE[best_pod]) > keep_size:\n                POD_KV_CACHE[best_pod] = set(list(POD_KV_CACHE[best_pod])[-keep_size:])\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nUnique approach: [Fragment formatting error: 'metric_name'], Comprehensive implementation\n\n### Inspiration 3 (Score: -11081.0482, Type: Exploratory)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load-aware tie-breaking\n        best_pods = []\n        best_score = -1\n        \n        # Track pod loads for tie-breaking\n        pod_loads = [0] * num_sims\n        for p in policy:\n            pod_loads[p] += 1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Choose least loaded pod among best candidates\n        best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route with exploration for partial matches\n        # If we have a partial match and multiple pods are underutilized,\n        # occasionally explore to spread cache\n        if best_score > 0 and best_score < len(block_keys) * 0.5:\n            pod_utilization = [len(POD_KV_CACHE[p]) for p in range(num_sims)]\n            min_util = min(pod_utilization)\n            if pod_utilization[best_pod] > min_util * 1.5:\n                # Find underutilized pods\n                underutilized = [p for p in range(num_sims) \n                               if pod_utilization[p] < min_util * 1.2]\n                if underutilized and len(policy) % 10 == 0:  # 10% exploration\n                    best_pod = underutilized[len(policy) % len(underutilized)]\n        \n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nUnique approach: [Fragment formatting error: 'metric_name'], Comprehensive implementation\n\n# Current Program\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n    pod_request_count = [0] * num_sims  # Track requests per pod\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie-breaking\n        best_pods = []\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0.0\n            # Weight earlier blocks more heavily (exponential decay)\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Earlier blocks get higher weight: 1.0, 0.9, 0.81, ...\n                    score += 0.9 ** idx\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) < 0.001:  # Float comparison tolerance\n                best_pods.append(pod_id)\n        \n        # Tie-breaking: choose least loaded pod among ties\n        if len(best_pods) > 1:\n            # Select pod with minimum requests so far\n            best_pod = min(best_pods, key=lambda p: pod_request_count[p])\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n        pod_request_count[best_pod] += 1  # Track pod usage\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Looking at the current program and its performance, I notice that the fitness score has plateaued at -11081.0482 across multiple attempts. The current implementation uses exponential decay weighting for prefix matching and load-based tie-breaking. Let me analyze potential improvements:\n\n1. **Cache Management**: The current implementation doesn't limit cache size, which could lead to memory issues and slower lookups as the cache grows.\n\n2. **Score Calculation**: The exponential decay (0.9^idx) might be too aggressive, potentially undervaluing later blocks that could still provide cache benefits.\n\n3. **Load Balancing**: The tie-breaking only considers request count, not the actual cache size or complexity of requests.\n\n4. **Exploration vs Exploitation**: There's no mechanism to occasionally explore less-used pods that might benefit from building up their cache.\n\nHere are my targeted improvements:\n\n<<<<<<< SEARCH\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0.0\n            # Weight earlier blocks more heavily (exponential decay)\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Earlier blocks get higher weight: 1.0, 0.9, 0.81, ...\n                    score += 0.9 ** idx\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) < 0.001:  # Float comparison tolerance\n                best_pods.append(pod_id)\n=======\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0.0\n            consecutive_hits = 0\n            # Use gentler decay and reward consecutive matches\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Gentler decay: 1.0, 0.95, 0.9025, ...\n                    # Plus bonus for consecutive matches\n                    score += (0.95 ** idx) * (1 + 0.1 * consecutive_hits)\n                    consecutive_hits += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) < 0.001:  # Float comparison tolerance\n                best_pods.append(pod_id)\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\n        # Tie-breaking: choose least loaded pod among ties\n        if len(best_pods) > 1:\n            # Select pod with minimum requests so far\n            best_pod = min(best_pods, key=lambda p: pod_request_count[p])\n        else:\n            best_pod = best_pods[0]\n=======\n        # Advanced tie-breaking considering both load and cache utilization\n        if len(best_pods) > 1:\n            # Consider both request count and cache size\n            def pod_load_score(p):\n                cache_penalty = len(POD_KV_CACHE[p]) / 1000.0  # Normalize cache size\n                return pod_request_count[p] + cache_penalty * 0.3\n            best_pod = min(best_pods, key=pod_load_score)\n        else:\n            best_pod = best_pods[0]\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n=======\n        # 5. Update KV cache with size management\n        POD_KV_CACHE[best_pod].update(block_keys)\n        \n        # Implement cache eviction if needed\n        max_cache_size = 3000  # Per-pod limit\n        if len(POD_KV_CACHE[best_pod]) > max_cache_size:\n            # Keep most recent 80% of cache\n            cache_list = list(POD_KV_CACHE[best_pod])\n            keep_size = int(max_cache_size * 0.8)\n            POD_KV_CACHE[best_pod] = set(cache_list[-keep_size:])\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n=======\n        # =========================\n        # Cold-start fairness with exploration\n        # =========================\n        if best_score == 0:\n            # Prefer least-used pods to build diverse caches\n            least_used_pods = [p for p in range(num_sims) \n                              if pod_request_count[p] == min(pod_request_count)]\n            best_pod = least_used_pods[len(policy) % len(least_used_pods)]\n        elif best_score < 2.0 and len(policy) % 20 == 0:\n            # Occasionally explore for low-scoring matches (5% of time)\n            underutilized = [p for p in range(num_sims) \n                           if pod_request_count[p] < sum(pod_request_count) / num_sims * 0.8]\n            if underutilized:\n                best_pod = underutilized[len(policy) % len(underutilized)]\n>>>>>>> REPLACE\n\n**Reasoning for improvements:**\n\n1. **Gentler exponential decay (0.95 vs 0.9)**: This gives more weight to later blocks in the sequence, which could still provide significant cache benefits. The consecutive match bonus rewards longer uninterrupted sequences.\n\n2. **Advanced tie-breaking**: Considers both request count and cache size, preventing pods from becoming overloaded with too many cache entries which could slow down lookups.\n\n3. **Cache eviction strategy**: Implements a maximum cache size per pod (3000 entries) with LRU-style eviction, keeping the most recent 80% when the limit is exceeded. This prevents unbounded cache growth.\n\n4. **Improved cold-start and exploration**: Instead of simple round-robin, it prefers least-used pods for cold starts to build diverse caches across pods. It also includes a 5% exploration rate for low-scoring matches to help build cache diversity.\n\nThese changes should help improve latency by:\n- Better utilizing cache across all pods\n- Preventing cache bloat that slows lookups\n- More accurately scoring prefix matches\n- Balancing load more effectively"]}}, "artifacts_json": "{\"avg_latency\": 11081.048222748814}", "artifact_dir": null, "embedding": null}