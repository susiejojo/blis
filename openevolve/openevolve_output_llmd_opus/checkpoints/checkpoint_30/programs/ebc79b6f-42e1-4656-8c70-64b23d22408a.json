{"id": "ebc79b6f-42e1-4656-8c70-64b23d22408a", "code": "\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router", "language": "python", "parent_id": "e1a10a5d-eab9-4478-af36-e980af6dcdbc", "generation": 3, "timestamp": 1770390032.227872, "iteration_found": 26, "metrics": {"combined_score": -15800.27639336493}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 17 lines with 13 lines\nChange 2: Replace 20 lines with 2 lines\nChange 3: Replace 6 lines with 3 lines\nChange 4: Replace 5 lines with ", "parent_metrics": {"combined_score": -12643.97489099526}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYou are optimizing a Python request router for large language model inference.\nThe router assigns each request to one of N pods by:\nTokenizing the prompt\nHashing tokens into KV blocks\nScoring pods by longest prefix match with their KV cache\nRouting to the pod with the highest score\nUpdating that pods KV cache after routing If no prefix match exists, a round-robin fallback is used.\nThe router is evaluated in a simulator that measures end-to-end latency (average and tail).\nGoal: The goal is to maximize the average reward returned by the simulators. You May\nModify the prefix scoring logic\nChange tie-breaking behavior\nAdd lightweight load-aware or probabilistic decisions\nIntroduce simple thresholds or heuristics\n", "user": "# Current Program Information\n- Fitness: -12643.9749\n- Feature coordinates: \n- Focus areas: - Fitness declined: -11081.0482 \u2192 -12643.9749. Consider revising recent changes.\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### avg_latency\n```\n12643.97489099526\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 10 lines with 11 lines\nChange 2: Replace 5 lines with 6 lines\nChange 3: Replace 5 lines with 5 lines\nChange 4: Replace 3 lines with 11 lines\nChange 5: Replace 2 lines with 3 lines\nChange 6: Replace 5 lines with 7 lines\n- Metrics: combined_score: -11081.0482\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace 14 lines with 25 lines\nChange 2: Replace # ================== with 15 lines\nChange 3: Replace 2 lines with 14 lines\n- Metrics: combined_score: -11081.0482\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace 14 lines with 32 lines\nChange 2: Replace 5 lines with 17 lines\nChange 3: Replace # ================== with 19 lines\n- Metrics: combined_score: -11081.0482\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with weighted blocks and tie-breaking\n        best_pods = []\n        best_score = -1\n        pod_scores = {}\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            # Weight earlier blocks more heavily\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Exponential decay: 1.0, 0.9, 0.81, ...\n                    score += 0.9 ** idx\n                else:\n                    break\n            pod_scores[pod_id] = score\n            \n            if score > best_score + 0.001:  # Small epsilon for float comparison\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) <= 0.001:\n                best_pods.append(pod_id)\n        \n        # Choose among ties based on current load\n        if len(best_pods) == 1:\n            best_pod = best_pods[0]\n        else:\n            # Count current assignments per pod\n            pod_loads = [0] * num_sims\n            for p in policy:\n                pod_loads[p] += 1\n            # Select least loaded pod among best candidates\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache with adaptive limit\n        # Add all blocks but manage total cache size\n        POD_KV_CACHE[best_pod].update(block_keys)\n        \n        # Implement cache eviction if needed (keep most recent)\n        max_cache_size = 5000  # Per-pod limit\n        if len(POD_KV_CACHE[best_pod]) > max_cache_size:\n            # Keep only the most recent entries\n            recent_blocks = set()\n            for prev_idx in range(max(0, len(policy) - 100), len(policy)):\n                if prev_idx < len(policy) and policy[prev_idx] == best_pod:\n                    # This is approximate - in practice would track actual blocks\n                    pass\n            # For now, just clear oldest entries by keeping 80% of cache\n            keep_size = int(max_cache_size * 0.8)\n            if len(POD_KV_CACHE[best_pod]) > keep_size:\n                POD_KV_CACHE[best_pod] = set(list(POD_KV_CACHE[best_pod])[-keep_size:])\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-11081.0482)\n\n### Program 2 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load-aware tie-breaking\n        best_pods = []\n        best_score = -1\n        \n        # Track pod loads for tie-breaking\n        pod_loads = [0] * num_sims\n        for p in policy:\n            pod_loads[p] += 1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Choose least loaded pod among best candidates\n        best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route with exploration for partial matches\n        # If we have a partial match and multiple pods are underutilized,\n        # occasionally explore to spread cache\n        if best_score > 0 and best_score < len(block_keys) * 0.5:\n            pod_utilization = [len(POD_KV_CACHE[p]) for p in range(num_sims)]\n            min_util = min(pod_utilization)\n            if pod_utilization[best_pod] > min_util * 1.5:\n                # Find underutilized pods\n                underutilized = [p for p in range(num_sims) \n                               if pod_utilization[p] < min_util * 1.2]\n                if underutilized and len(policy) % 10 == 0:  # 10% exploration\n                    best_pod = underutilized[len(policy) % len(underutilized)]\n        \n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-11081.0482)\n\n### Program 3 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache and track pod loads\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n    \n    pod_loads = [0] * num_sims\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie tracking\n        best_pods = []\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Exponentially decay weight for later blocks\n                    score += 1.0 / (1.0 + idx * 0.1)\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Choose least loaded pod among ties\n        if len(best_pods) == 1:\n            best_pod = best_pods[0]\n        else:\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route and update load\n        policy.append(best_pod)\n        pod_loads[best_pod] += 1\n\n        # 5. Update KV cache with adaptive limit\n        # Allow more blocks for longer requests\n        new_blocks = [k for k in block_keys if k not in POD_KV_CACHE[best_pod]]\n        if new_blocks:\n            # Adaptive limit: at least 5, at most 20, based on request length\n            limit = min(20, max(5, len(block_keys) // 2))\n            POD_KV_CACHE[best_pod].update(new_blocks[:limit])\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-11081.0482)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {i: set() for i in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Alternative approach to combined_score\n\n### Program D2 (Score: -11081.0482)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n    pod_request_count = [0] * num_sims  # Track requests per pod\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie-breaking\n        best_pods = []\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0.0\n            # Weight early blocks more heavily for better cache locality\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Exponential decay: first block worth 1.0, second 0.8, third 0.64, etc\n                    score += 0.8 ** idx\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Tie-breaking: choose least loaded pod among ties\n        if len(best_pods) > 1:\n            # Select pod with minimum requests so far\n            best_pod = min(best_pods, key=lambda p: pod_request_count[p])\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n        pod_request_count[best_pod] += 1  # Track pod usage\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -11081.0482, Type: Exploratory)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nUnique approach: [Fragment formatting error: 'metric_name'], Comprehensive implementation\n\n### Inspiration 2 (Score: -11081.0482, Type: Exploratory)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with weighted blocks and tie-breaking\n        best_pods = []\n        best_score = -1\n        pod_scores = {}\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            # Weight earlier blocks more heavily\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Exponential decay: 1.0, 0.9, 0.81, ...\n                    score += 0.9 ** idx\n                else:\n                    break\n            pod_scores[pod_id] = score\n            \n            if score > best_score + 0.001:  # Small epsilon for float comparison\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) <= 0.001:\n                best_pods.append(pod_id)\n        \n        # Choose among ties based on current load\n        if len(best_pods) == 1:\n            best_pod = best_pods[0]\n        else:\n            # Count current assignments per pod\n            pod_loads = [0] * num_sims\n            for p in policy:\n                pod_loads[p] += 1\n            # Select least loaded pod among best candidates\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache with adaptive limit\n        # Add all blocks but manage total cache size\n        POD_KV_CACHE[best_pod].update(block_keys)\n        \n        # Implement cache eviction if needed (keep most recent)\n        max_cache_size = 5000  # Per-pod limit\n        if len(POD_KV_CACHE[best_pod]) > max_cache_size:\n            # Keep only the most recent entries\n            recent_blocks = set()\n            for prev_idx in range(max(0, len(policy) - 100), len(policy)):\n                if prev_idx < len(policy) and policy[prev_idx] == best_pod:\n                    # This is approximate - in practice would track actual blocks\n                    pass\n            # For now, just clear oldest entries by keeping 80% of cache\n            keep_size = int(max_cache_size * 0.8)\n            if len(POD_KV_CACHE[best_pod]) > keep_size:\n                POD_KV_CACHE[best_pod] = set(list(POD_KV_CACHE[best_pod])[-keep_size:])\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nUnique approach: [Fragment formatting error: 'metric_name'], Comprehensive implementation\n\n### Inspiration 3 (Score: -1000000000.0000, Type: Exploratory)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n    pod_request_count = [0] * num_sims  # Track requests per pod\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie-breaking\n        best_pods = []\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0.0\n            consecutive_matches = 0\n            # Use gentler decay that rewards longer consecutive matches\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Linear decay with bonus for consecutive matches\n                    # First block: 1.0, second: 0.95, third: 0.90, etc.\n                    base_weight = max(0.1, 1.0 - idx * 0.05)\n                    # Bonus for consecutive matches to encourage cache locality\n                    consecutive_bonus = 1.0 + consecutive_matches * 0.1\n                    score += base_weight * consecutive_bonus\n                    consecutive_matches += 1\n                else:\n                    break\n=======\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif abs(score - best_score) < 0.001:  # Float comparison tolerance\n                best_pods.append(pod_id)\n        \n        # Tie-breaking: consider both request count and cache size\n        if len(best_pods) > 1:\n            # Compute composite load score considering requests and cache size\n            def pod_load_score(p):\n                request_load = pod_request_count[p] / max(1, sum(pod_request_count))\n                cache_load = len(POD_KV_CACHE[p]) / max(1000, sum(len(POD_KV_CACHE[pid]) for pid in range(num_sims)))\n                # Weight request count more heavily than cache size\n                return 0.7 * request_load + 0.3 * cache_load\n            \n            best_pod = min(best_pods, key=pod_load_score)\n        else:\n            best_pod = best_pods[0]\n=======\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n        pod_request_count[best_pod] += 1  # Track pod usage\n\n        # 5. Update KV cache AFTER routing with adaptive strategy\n        cache_size = len(POD_KV_CACHE[best_pod])\n        if cache_size < 5000:\n            # Small cache: add all blocks\n            POD_KV_CACHE[best_pod].update(block_keys)\n        elif cache_size < 10000:\n            # Medium cache: prioritize early blocks\n            POD_KV_CACHE[best_pod].update(block_keys[:max(1, len(block_keys) * 2 // 3)])\n        else:\n            # Large cache: only add the most important early blocks\n            POD_KV_CACHE[best_pod].update(block_keys[:max(1, len(block_keys) // 2)])\n=======\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nUnique approach: [Fragment formatting error: 'metric_name'], Comprehensive implementation\n\n# Current Program\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Enhanced prefix scoring with position weighting\n        best_pods = []\n        best_score = -1\n        \n        # Track current pod loads for better tie-breaking\n        pod_loads = [sum(1 for p in policy if p == i) for i in range(num_sims)]\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            consecutive_matches = 0\n            \n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Give higher weight to early consecutive matches\n                    consecutive_matches += 1\n                    score += (2.0 - idx * 0.1) * consecutive_matches\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Choose least loaded pod among ties\n        if len(best_pods) == 1:\n            best_pod = best_pods[0]\n        else:\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Smart KV cache update with adaptive limits\n        # Add all blocks for high-scoring matches, limit for low scores\n        new_blocks = [k for k in block_keys if k not in POD_KV_CACHE[best_pod]]\n        \n        if new_blocks:\n            # If we had a good match, cache more aggressively\n            if best_score > len(block_keys) * 0.5:\n                POD_KV_CACHE[best_pod].update(new_blocks)\n            else:\n                # For poor matches, limit cache growth based on pod utilization\n                cache_size = len(POD_KV_CACHE[best_pod])\n                avg_cache_size = sum(len(cache) for cache in POD_KV_CACHE.values()) / num_sims\n                \n                # Allow more caching for underutilized pods\n                if cache_size < avg_cache_size * 0.8:\n                    limit = min(len(new_blocks), 15)\n                else:\n                    limit = min(len(new_blocks), 5)\n                \n                POD_KV_CACHE[best_pod].update(new_blocks[:limit])\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Looking at the current program, I see it has a fitness score of -12643.9749, which is worse than the previous attempts that achieved -11081.0482. The key insight is that the current program has become overly complex, particularly in the scoring logic and cache management. Let me analyze the issues and suggest targeted improvements:\n\n1. **Overly Complex Scoring**: The consecutive matching bonus with position weighting (`(2.0 - idx * 0.1) * consecutive_matches`) may be causing poor routing decisions\n2. **Inefficient Cache Management**: The adaptive caching logic with multiple conditions adds overhead without clear benefit\n3. **Code Length**: The program exceeds 500 characters, which was flagged as a concern\n\nLet me simplify the approach while maintaining the core functionality that worked well in the top-performing programs:\n\n<<<<<<< SEARCH\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            consecutive_matches = 0\n            \n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Give higher weight to early consecutive matches\n                    consecutive_matches += 1\n                    score += (2.0 - idx * 0.1) * consecutive_matches\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n=======\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n>>>>>>> REPLACE\n\nThis simplification removes the complex consecutive matching bonus that was likely causing suboptimal routing decisions. The simple longest-prefix match worked well in the top-performing programs.\n\n<<<<<<< SEARCH\n        # 5. Smart KV cache update with adaptive limits\n        # Add all blocks for high-scoring matches, limit for low scores\n        new_blocks = [k for k in block_keys if k not in POD_KV_CACHE[best_pod]]\n        \n        if new_blocks:\n            # If we had a good match, cache more aggressively\n            if best_score > len(block_keys) * 0.5:\n                POD_KV_CACHE[best_pod].update(new_blocks)\n            else:\n                # For poor matches, limit cache growth based on pod utilization\n                cache_size = len(POD_KV_CACHE[best_pod])\n                avg_cache_size = sum(len(cache) for cache in POD_KV_CACHE.values()) / num_sims\n                \n                # Allow more caching for underutilized pods\n                if cache_size < avg_cache_size * 0.8:\n                    limit = min(len(new_blocks), 15)\n                else:\n                    limit = min(len(new_blocks), 5)\n                \n                POD_KV_CACHE[best_pod].update(new_blocks[:limit])\n=======\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n>>>>>>> REPLACE\n\nThis removes the complex adaptive caching logic. The simple approach of caching all blocks worked effectively in the top performers. The complex logic was adding computational overhead without demonstrable benefit.\n\n<<<<<<< SEARCH\n        # 3. Enhanced prefix scoring with position weighting\n        best_pods = []\n        best_score = -1\n        \n        # Track current pod loads for better tie-breaking\n        pod_loads = [sum(1 for p in policy if p == i) for i in range(num_sims)]\n=======\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n>>>>>>> REPLACE\n\nThis simplifies the initialization and removes the expensive pod load calculation that recomputes the entire policy list for each request.\n\n<<<<<<< SEARCH\n        # Choose least loaded pod among ties\n        if len(best_pods) == 1:\n            best_pod = best_pods[0]\n        else:\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n=======\n>>>>>>> REPLACE\n\nSince we're now tracking the best pod directly instead of a list of tied pods, we can remove this tie-breaking logic entirely.\n\nThese changes bring the program back to a simpler, more efficient design similar to the top-performing programs. The key improvements are:\n\n1. **Simplified scoring**: Return to basic longest-prefix matching without complex weighting\n2. **Efficient caching**: Cache all blocks without complex adaptive logic\n3. **Reduced overhead**: Remove expensive load calculations and tie-breaking logic\n4. **Shorter code**: Significantly reduce code length to address the >500 character concern\n\nThis should improve performance by reducing computational overhead while maintaining the effective prefix-matching strategy that worked well in previous attempts."]}}, "artifacts_json": "{\"avg_latency\": 15800.27639336493}", "artifact_dir": null, "embedding": null}