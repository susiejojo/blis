{"id": "a8796463-b3cc-442e-89c2-963471af8b30", "code": "\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load awareness\n        best_pod = 0\n        best_score = -1\n        pod_loads = [0] * num_sims\n        \n        # Count recent assignments (simple load metric)\n        for i, p in enumerate(policy[-20:]):  # Look at last 20 assignments\n            pod_loads[p] += 1\n        \n        candidates = []\n        \n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            # Collect all pods with scores close to best\n            if score > best_score:\n                best_score = score\n                candidates = [(pod_id, score)]\n            elif score == best_score:\n                candidates.append((pod_id, score))\n        \n        # Among candidates with same score, pick least loaded\n        if len(candidates) > 1:\n            best_pod = min(candidates, key=lambda x: pod_loads[x[0]])[0]\n        else:\n            best_pod = candidates[0][0] if candidates else 0\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Probabilistic routing for exploration (only for partial matches)\n        if 0 < best_score < len(block_keys) * 0.8:  # Partial match threshold\n            # 5% chance to explore a different pod with some cache hits\n            import random\n            if random.random() < 0.05:\n                alternate_pods = [p for p, _ in candidates[1:] if p != best_pod]\n                if alternate_pods:\n                    best_pod = random.choice(alternate_pods)\n        \n        # 5. Route\n        policy.append(best_pod)\n\n        # 6. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router", "language": "python", "parent_id": "9b0cea97-f378-4f50-bd37-e2bbc519fbef", "generation": 1, "timestamp": 1770390450.753819, "iteration_found": 5, "metrics": {"combined_score": -4031.4237688679245}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 14 lines with 31 lines\nChange 2: Replace # ================== with 17 lines\nChange 3: Replace 5 lines with 14 lines", "parent_metrics": {"combined_score": -4031.4237688679245}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYou are optimizing a Python request router for large language model inference.\nThe router assigns each request to one of N pods by:\nTokenizing the prompt\nHashing tokens into KV blocks\nScoring pods by longest prefix match with their KV cache\nRouting to the pod with the highest score\nUpdating that pods KV cache after routing If no prefix match exists, a round-robin fallback is used.\nThe router is evaluated in a simulator that measures end-to-end latency (average and tail).\nGoal: The goal is to maximize the average reward returned by the simulators. You May\nModify the prefix scoring logic\nChange tie-breaking behavior\nAdd lightweight load-aware or probabilistic decisions\nIntroduce simple thresholds or heuristics\n", "user": "# Current Program Information\n- Fitness: -4031.4238\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at -4031.4238\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: combined_score: -4031.4238\n- Outcome: Regression in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -4031.4238)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-4031.4238)\n\n\n\n# Current Program\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Looking at the current program and its fitness score of -4031.4238, I need to analyze potential improvements to the prefix-aware router. The key areas to optimize are:\n\n1. **Load balancing**: The current approach may lead to imbalanced pod usage\n2. **Cache efficiency**: Better utilization of existing cache entries\n3. **Tie-breaking strategy**: When multiple pods have the same score\n\nLet me suggest targeted improvements:\n\n## Improvement 1: Add load-aware routing with exponential decay\n\nWhen multiple pods have the same prefix score, we should consider their current load to distribute requests more evenly. This prevents hot spots where one pod gets overloaded.\n\n<<<<<<< SEARCH\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n=======\n        # 3. Longest-prefix scoring with load awareness\n        best_pod = 0\n        best_score = -1\n        pod_loads = [0] * num_sims\n        \n        # Count recent assignments (simple load metric)\n        for i, p in enumerate(policy[-20:]):  # Look at last 20 assignments\n            pod_loads[p] += 1\n        \n        candidates = []\n        \n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            # Collect all pods with scores close to best\n            if score > best_score:\n                best_score = score\n                candidates = [(pod_id, score)]\n            elif score == best_score:\n                candidates.append((pod_id, score))\n        \n        # Among candidates with same score, pick least loaded\n        if len(candidates) > 1:\n            best_pod = min(candidates, key=lambda x: pod_loads[x[0]])[0]\n        else:\n            best_pod = candidates[0][0] if candidates else 0\n>>>>>>> REPLACE\n\n## Improvement 2: Improve cold-start distribution\n\nInstead of simple round-robin for cold starts (score=0), use a hash-based distribution that's more likely to group similar requests together.\n\n<<<<<<< SEARCH\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n=======\n        # =========================\n        # Cold-start with hash-based distribution\n        # =========================\n        if best_score == 0:\n            # Use first block key to distribute cold requests\n            if block_keys:\n                # Hash-based distribution for better locality\n                best_pod = int(hashlib.sha256(block_keys[0].encode()).hexdigest()[:8], 16) % num_sims\n            else:\n                # Fallback to round-robin if no blocks\n                best_pod = len(policy) % num_sims\n>>>>>>> REPLACE\n\n## Improvement 3: Add probabilistic routing for partial matches\n\nWhen we have partial prefix matches (score > 0 but not complete), we can add a small amount of randomness to explore other pods that might develop better cache patterns over time.\n\n<<<<<<< SEARCH\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n=======\n        # 4. Probabilistic routing for exploration (only for partial matches)\n        if 0 < best_score < len(block_keys) * 0.8:  # Partial match threshold\n            # 5% chance to explore a different pod with some cache hits\n            import random\n            if random.random() < 0.05:\n                alternate_pods = [p for p, _ in candidates[1:] if p != best_pod]\n                if alternate_pods:\n                    best_pod = random.choice(alternate_pods)\n        \n        # 5. Route\n        policy.append(best_pod)\n\n        # 6. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys)\n>>>>>>> REPLACE\n\n## Rationale for improvements:\n\n1. **Load-aware tie-breaking**: When multiple pods have the same prefix match score, choosing the least recently used pod helps distribute load more evenly, reducing tail latency.\n\n2. **Hash-based cold-start distribution**: Using a hash of the first block key for cold starts creates better locality - similar prompts are more likely to be routed to the same pod, improving future cache hits.\n\n3. **Probabilistic exploration**: Adding a small amount of randomness (5%) for partial matches allows the system to discover potentially better cache patterns while still mostly following the greedy strategy. This is only applied to partial matches to avoid disrupting good complete matches.\n\nThese changes maintain the core prefix-matching algorithm while adding lightweight enhancements that should improve both average and tail latency through better load distribution and cache utilization."]}}, "artifacts_json": "{\"avg_latency\": 4031.4237688679245}", "artifact_dir": null, "embedding": null}