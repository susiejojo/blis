{"id": "d6f42771-7e49-4cf8-9cbd-4dfece5397b2", "code": "# EVOLVE-BLOCK-START\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state for round-robin using function attributes.\n    # This state is intended to persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    \n    policy = []\n    for req in requests:\n        # Assign the current request to the next simulator in a round-robin fashion.\n        sim_id = router.next_sim_idx\n        policy.append(sim_id)\n        \n        # Update the next simulator index for the next request.\n        router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n", "language": "python", "parent_id": "259748d8-6b34-42c5-99fa-3baef4bdd675", "generation": 1, "timestamp": 1768592399.425982, "iteration_found": 8, "metrics": {"combined_score": -90.31099750000001}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 11 lines with 19 lines", "parent_metrics": {"combined_score": -109.2890875}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYour task is to improve a routing algorithm that assigns requests to multiple simulator instances.\nThe algorithm receives a list of requests and must decide which simulator each request should be routed to.\nThe goal is to maximize the average reward returned by the simulators.\nGood routing solutions often balance load while preserving request locality (e.g., repeated or similar requests benefiting from reuse).\nFocus only on improving the routing logic. Do not change the evaluator or any external code.\n", "user": "# Current Program Information\n- Fitness: -109.2891\n- Feature coordinates: \n- Focus areas: - Fitness improved: -1000000000.0000 \u2192 -109.2891\n- No feature coordinates\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 13 lines with 40 lines\n- Metrics: score: -1000000000.0000\n- Outcome: Regression in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace 13 lines with 40 lines\n- Metrics: score: -1000000000.0000\n- Outcome: Regression in all metrics\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: combined_score: -109.2891\n- Outcome: Regression in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -109.2891)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-109.2891)\n\n### Program 2 (Score: -1000000000.0000)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on score (-1000000000.0000)\n\n### Program 3 (Score: -1000000000.0000)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on score (-1000000000.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -1000000000.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -1000000000.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current top-performing program uses a random assignment policy, which offers average load balancing but no request locality. Previous attempts to implement sticky sessions (Programs 2, 3, and Inspirations) failed catastrophically with a score of -1000000000.0000. This strongly suggests a runtime error. The most likely cause for such an error in those programs is that the `req` object is not hashable, leading to a `TypeError` when used as a dictionary key in `router.request_sim_map`.\n\nTo improve upon the random policy while avoiding the hashability issue, the most robust next step is to implement a **Round-Robin (RR)** load balancing strategy.\n\n**Proposed Change:** Replace the random assignment with a round-robin logic.\n\n**Reasoning:**\n\n1.  **Addresses Previous Failure Mode:** The Round-Robin strategy does not rely on `req` objects being hashable. It only uses the requests in an iterative manner, thus avoiding the `TypeError` that likely caused the failure of prior sticky-session attempts.\n2.  **Improved Load Balancing:** Unlike random assignment, round-robin guarantees an even distribution of requests across all `num_sims` simulators over time. This leads to more predictable and efficient resource utilization, directly contributing to the goal of maximizing average reward by ensuring no simulator is disproportionately overloaded while others are idle.\n3.  **Persistence of State (Assuming Evaluator Behavior):** The `next_sim_idx` variable is stored as a function attribute (`router.next_sim_idx`). If the evaluation environment allows this state to persist across multiple calls to the `router` function within a single test run, it will ensure long-term, global load balancing. This is a common pattern for maintaining \"static\" state in functions.\n4.  **Simplicity and Robustness:** Round-robin is a simple, well-understood algorithm that is robust and doesn't require complex data structures or assumptions about the input `req` type beyond it being iterable.\n5.  **Stepping Stone:** While RR doesn't provide specific request-based locality, it's a significant improvement in load balancing over pure randomness and allows us to validate the state persistence mechanism (`hasattr(router, ...)`) without the complication of `req` hashability. If this succeeds, future iterations could explore locality with a known hashable identifier for `req` (if one exists).\n\nThis change improves load distribution, which is a key component of maximizing reward in a multi-simulator environment.\n\n<<<<<<< SEARCH\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n=======\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state for round-robin using function attributes.\n    # This state is intended to persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    \n    policy = []\n    for req in requests:\n        # Assign the current request to the next simulator in a round-robin fashion.\n        sim_id = router.next_sim_idx\n        policy.append(sim_id)\n        \n        # Update the next simulator index for the next request.\n        router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n        \n    return policy\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"avg_latency\": 90.31099750000001, \"sim0_requests\": 200, \"sim1_requests\": 200}", "artifact_dir": null, "embedding": null}