{"id": "d29887e3-0a04-48dd-a98e-083c84023761", "code": "# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        request_key = req.id # Extract a hashable identifier for locality.\n        # This assumes 'InferenceRequest' objects have a hashable 'id' attribute.\n        if request_key in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[request_key] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[request_key] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n", "language": "python", "parent_id": "5d1bccfd-7b85-46ce-a407-0efe1e934268", "generation": 2, "timestamp": 1768592439.530041, "iteration_found": 14, "metrics": {"score": -1000000000.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 3 lines with 4 lines\nChange 2: 'router.request_sim_map[req] = sim_id # Update with new valid assignment.' to 'router.request_sim_map[request_key] = sim_id # Update with new valid assignment.'\nChange 3: 'router.request_sim_map[req] = sim_id # Store for future requests.' to 'router.request_sim_map[request_key] = sim_id # Store for future requests.'", "parent_metrics": {"score": -1000000000.0}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYour task is to improve a routing algorithm that assigns requests to multiple simulator instances.\nThe algorithm receives a list of requests and must decide which simulator each request should be routed to.\nThe goal is to maximize the average reward returned by the simulators.\nGood routing solutions often balance load while preserving request locality (e.g., repeated or similar requests benefiting from reuse).\nFocus only on improving the routing logic. Do not change the evaluator or any external code.\n", "user": "# Current Program Information\n- Fitness: -1000000000.0000\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at -1000000000.0000\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### error\n```\nunhashable type: 'InferenceRequest'\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 3 lines with 5 lines\nChange 2: 'router.request_sim_map[req] = sim_id # Update with new valid assignment.' to 'router.request_sim_map[request_key] = sim_id # Update with new valid assignment.'\nChange 3: 'router.request_sim_map[req] = sim_id # Store for future requests.' to 'router.request_sim_map[request_key] = sim_id # Store for future requests.'\n- Metrics: score: -1000000000.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: combined_score: -109.2891\n- Outcome: Regression in all metrics\n\n### Attempt 1\n- Changes: Change 1: Replace 13 lines with 22 lines\n- Metrics: combined_score: -90.3110\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -90.3110)\n```python\n# EVOLVE-BLOCK-START\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state for round-robin.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    \n    policy = []\n    for req in requests:\n        # Assign current request to the next simulator in a round-robin fashion.\n        sim_id = router.next_sim_idx\n        policy.append(sim_id)\n        \n        # Update the next_sim_idx for the subsequent request.\n        # This handles cases where num_sims might change between calls.\n        router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-90.3110)\n\n### Program 2 (Score: -109.2891)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-109.2891)\n\n### Program 3 (Score: -1000000000.0000)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # Assuming 'InferenceRequest' objects have a hashable 'id' attribute\n        # that serves as a stable identifier for locality (e.g., for similar requests).\n        request_key = req.id \n        if request_key in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[request_key]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[request_key] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[request_key] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on score (-1000000000.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -1000000000.0000)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to score\n\n### Program D2 (Score: -1000000000.0000)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -90.3110, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state for round-robin.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    \n    policy = []\n    for req in requests:\n        # Assign current request to the next simulator in a round-robin fashion.\n        sim_id = router.next_sim_idx\n        policy.append(sim_id)\n        \n        # Update the next_sim_idx for the subsequent request.\n        # This handles cases where num_sims might change between calls.\n        router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -1000000000.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -1000000000.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program is failing with a `TypeError: unhashable type: 'InferenceRequest'`. This error occurs because `InferenceRequest` objects are being used directly as dictionary keys in `router.request_sim_map`. Python dictionary keys must be hashable. Custom objects are not hashable by default unless `__hash__` and `__eq__` methods are explicitly defined.\n\nThe problem description and the comments in various programs (e.g., Program 3, D1) explicitly suggest that `InferenceRequest` objects are assumed to have a hashable `id` attribute that can serve as a stable identifier for locality.\n\nTherefore, the most direct and necessary improvement is to use `req.id` as the key for `router.request_sim_map` instead of the `req` object itself. This will resolve the `TypeError` and allow the intended locality-preserving logic to function.\n\nHere are the changes:\n\n1.  **Introduce `request_key = req.id`**: Define `request_key` using `req.id` at the beginning of the loop.\n2.  **Replace `req` with `request_key` in dictionary lookups**: Change `if req in router.request_sim_map:` to `if request_key in router.request_sim_map:`.\n3.  **Replace `req` with `request_key` in dictionary assignments**: Change `router.request_sim_map[req] = sim_id` to `router.request_sim_map[request_key] = sim_id` in both assignment locations.\n4.  **Update comments**: Clarify that `req.id` is being used as the hashable identifier.\n\nThese changes will enable the locality feature to work as intended, which is crucial for maximizing average reward by leveraging repeated or similar requests.\n\n<<<<<<< SEARCH\n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n=======\n        # Attempt to use existing mapping for locality (sticky sessions).\n        request_key = req.id # Extract a hashable identifier for locality.\n        # This assumes 'InferenceRequest' objects have a hashable 'id' attribute.\n        if request_key in router.request_sim_map:\n>>>>>>> REPLACE\n<<<<<<< SEARCH\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n=======\n                router.request_sim_map[request_key] = sim_id # Update with new valid assignment.\n>>>>>>> REPLACE\n<<<<<<< SEARCH\n            router.request_sim_map[req] = sim_id # Store for future requests.\n=======\n            router.request_sim_map[request_key] = sim_id # Store for future requests.\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"error\": \"'InferenceRequest' object has no attribute 'id'\"}", "artifact_dir": null, "embedding": null}