{"id": "f12f4cf6-a80d-4044-b985-bf41ebd0e030", "code": "# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # To enable locality, we need a hashable key derived from the request object.\n        # Assuming 'req' (an InferenceRequest object) has a 'key' attribute\n        # that provides a stable, hashable identifier for similar requests.\n        request_key = req.key\n        \n        # Attempt to use existing mapping for locality (sticky sessions) with request_key.\n        if request_key in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[request_key]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[request_key] = sim_id # Update with new valid assignment.\n        else:\n            # If the request_key is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[request_key] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n", "language": "python", "parent_id": "8eaea806-f8ee-4030-93ad-0b9869d42f81", "generation": 3, "timestamp": 1768592499.614905, "iteration_found": 20, "metrics": {"score": -1000000000.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace request_key = req.key with 7 lines", "parent_metrics": {"score": -1000000000.0}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYour task is to improve a routing algorithm that assigns requests to multiple simulator instances.\nThe algorithm receives a list of requests and must decide which simulator each request should be routed to.\nThe goal is to maximize the average reward returned by the simulators.\nGood routing solutions often balance load while preserving request locality (e.g., repeated or similar requests benefiting from reuse).\nFocus only on improving the routing logic. Do not change the evaluator or any external code.\n", "user": "# Current Program Information\n- Fitness: -1000000000.0000\n- Feature coordinates: \n- Focus areas: - Fitness declined: -109.2891 \u2192 -1000000000.0000. Consider revising recent changes.\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### error\n```\n'InferenceRequest' object has no attribute 'key'\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Unknown changes\n- Metrics: combined_score: -109.2891\n- Outcome: Regression in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace 11 lines with 19 lines\n- Metrics: combined_score: -90.3110\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Change 1: Replace 13 lines with 22 lines\n- Metrics: combined_score: -90.3110\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -90.3110)\n```python\n# EVOLVE-BLOCK-START\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state for round-robin.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    \n    policy = []\n    for req in requests:\n        # Assign current request to the next simulator in a round-robin fashion.\n        sim_id = router.next_sim_idx\n        policy.append(sim_id)\n        \n        # Update the next_sim_idx for the subsequent request.\n        # This handles cases where num_sims might change between calls.\n        router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-90.3110)\n\n### Program 2 (Score: -90.3110)\n```python\n# EVOLVE-BLOCK-START\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state for round-robin using function attributes.\n    # This state is intended to persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    \n    policy = []\n    for req in requests:\n        # Assign the current request to the next simulator in a round-robin fashion.\n        sim_id = router.next_sim_idx\n        policy.append(sim_id)\n        \n        # Update the next simulator index for the next request.\n        router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-90.3110)\n\n### Program 3 (Score: -109.2891)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-109.2891)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -1000000000.0000)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # Assuming 'InferenceRequest' objects have a hashable 'id' attribute\n        # that serves as a stable identifier for locality (e.g., for similar requests).\n        request_key = req.id \n        if request_key in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[request_key]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[request_key] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[request_key] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to score\n\n### Program D2 (Score: -1000000000.0000)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -90.3110, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state for round-robin.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    \n    policy = []\n    for req in requests:\n        # Assign current request to the next simulator in a round-robin fashion.\n        sim_id = router.next_sim_idx\n        policy.append(sim_id)\n        \n        # Update the next_sim_idx for the subsequent request.\n        # This handles cases where num_sims might change between calls.\n        router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -1000000000.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # Assuming 'InferenceRequest' objects have a hashable 'id' attribute\n        # that serves as a stable identifier for locality (e.g., for similar requests).\n        request_key = req.id \n        if request_key in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[request_key]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[request_key] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[request_key] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -1000000000.0000, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # Attempt to use existing mapping for locality (sticky sessions).\n        # This assumes 'req' is hashable and serves as a stable identifier.\n        if req in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[req]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[req] = sim_id # Update with new valid assignment.\n        else:\n            # If the request is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[req] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\n# import random # Not used in this improved policy\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    # Initialize persistent state using function attributes.\n    # This state will persist across calls to the 'router' function instance.\n    if not hasattr(router, 'next_sim_idx'):\n        router.next_sim_idx = 0\n    if not hasattr(router, 'request_sim_map'):\n        router.request_sim_map = {}\n    \n    policy = []\n    for req in requests:\n        sim_id = None\n        \n        # To enable locality, we need a hashable key derived from the request object.\n        # Assuming 'req' (an InferenceRequest object) has a 'key' attribute\n        # that provides a stable, hashable identifier for similar requests.\n        request_key = req.key\n        \n        # Attempt to use existing mapping for locality (sticky sessions) with request_key.\n        if request_key in router.request_sim_map:\n            cached_sim_id = router.request_sim_map[request_key]\n            # Check if the cached sim_id is still valid for the current num_sims.\n            # If num_sims has decreased and the cached ID is now out of bounds, re-assign.\n            if cached_sim_id < num_sims:\n                sim_id = cached_sim_id\n            else:\n                # Re-assign using round-robin if the cached ID is invalid (e.g., due to num_sims reduction).\n                sim_id = router.next_sim_idx\n                router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n                router.request_sim_map[request_key] = sim_id # Update with new valid assignment.\n        else:\n            # If the request_key is not seen before, assign using round-robin for load balancing.\n            sim_id = router.next_sim_idx\n            router.next_sim_idx = (router.next_sim_idx + 1) % num_sims\n            router.request_sim_map[request_key] = sim_id # Store for future requests.\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program fails with an `AttributeError: 'InferenceRequest' object has no attribute 'key'`. This indicates that the `InferenceRequest` objects passed to the `router` function do not possess a `key` attribute as assumed by the current implementation. This error causes the program to crash, leading to the lowest possible fitness score (-1000000000.0000).\n\nThe problem statement emphasizes \"preserving request locality (e.g., repeated or similar requests benefiting from reuse).\" To achieve this, we need a stable, hashable identifier for each request or class of similar requests. Previous attempts (D1, D2) trying `req.id` or `req` directly also resulted in errors, suggesting that `InferenceRequest` objects might not have a suitable `id` attribute, or they are not hashable by default, or that distinct objects are created even for \"similar\" requests, making object identity unreliable for locality.\n\nWithout explicit information about the structure of the `InferenceRequest` class, the most robust way to derive a key that will not cause an `AttributeError` and might still capture \"similarity\" for locality is to hash its `repr()` string representation. While `repr(obj)` may not always yield a string perfectly suited for identifying request similarity across different object instances, it is a safe fallback that avoids crashes and might work if `InferenceRequest`'s `__repr__` method includes relevant state. If `repr(obj)` typically includes memory addresses (e.g., `<__main__.InferenceRequest object at 0x...>`), this approach will fall back to per-object instance caching rather than true \"similar request\" caching, but it will still allow the program to run and potentially improve over simple round-robin if the same object instances are frequently \"repeated\".\n\nThe rest of the routing logic, which handles sticky sessions, round-robin assignment for new requests, and gracefully managing changes in `num_sims`, is generally sound. The immediate priority is to fix the crash.\n\n**Proposed Change:**\n\nReplace the line `request_key = req.key` with a more robust way to derive a hashable key from the `InferenceRequest` object, specifically `hash(repr(req))`.\n\n<<<<<<< SEARCH\n            request_key = req.key\n=======\n            # To enable locality, we need a hashable key derived from the request object.\n            # The 'InferenceRequest' object does not have a 'key' attribute.\n            # As a robust fallback, we use hash(repr(req)). This attempts to\n            # provide locality based on the object's representation string.\n            # This will prevent the AttributeError and allow the routing logic\n            # to function, though optimal locality depends on a meaningful repr().\n            request_key = hash(repr(req))\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"error\": \"'InferenceRequest' object has no attribute 'key'\"}", "artifact_dir": null, "embedding": null}