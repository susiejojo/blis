{"id": "f75221df-2b55-431b-919b-69fb9a082a19", "code": "# EVOLVE-BLOCK-START\nimport random # Not used in this specific version, but kept for potential future variations.\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version routes requests based on a deterministic hash of a common prefix\n    extracted from request.input. It aims to maximize locality by routing requests\n    with similar prefixes to the same simulator, while also balancing load across simulators.\n\n    The prefix (routing_key) is heuristically defined as:\n    - The request.input string up to the last '/' character, if a '/' is present.\n    - The entire request.input string, if no '/' is found.\n\n    This strategy assumes that the \"shared prefix\" benefit applies to hierarchical\n    or path-like inputs where related requests share a common parent segment.\n    \"\"\"\n    policy = []\n    \n    # This dictionary maps extracted routing keys (prefixes) to simulator IDs.\n    # It ensures that all requests with the same routing_key within this batch\n    # are assigned to the same simulator, preserving locality.\n    prefix_to_sim_map = {} \n    \n    # Tracks the number of requests assigned to each simulator in the current batch.\n    # Used to make load-aware decisions for new prefixes.\n    sim_loads = [0] * num_sims\n\n    for req in requests:\n        # Ensure req.input is treated as a string for consistent hashing.\n        input_data = str(req.input)\n\n        # Determine the routing key based on a common prefix.\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        # Assign a simulator ID to this routing key, preserving locality if seen before.\n        assigned_sim_id = None\n        if routing_key in prefix_to_sim_map:\n            assigned_sim_id = prefix_to_sim_map[routing_key]\n        else:\n            # For new prefixes, assign to the currently least loaded simulator.\n            least_loaded_sim_id = 0\n            min_load = sim_loads[0]\n            for i in range(1, num_sims):\n                if sim_loads[i] < min_load:\n                    min_load = sim_loads[i]\n                    least_loaded_sim_id = i\n            \n            assigned_sim_id = least_loaded_sim_id\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n        \n        # Update load for the assigned simulator and append to policy.\n        sim_loads[assigned_sim_id] += 1\n        policy.append(assigned_sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n", "language": "python", "parent_id": "e8babf95-3c06-4a02-9273-53dd5276b0a2", "generation": 3, "timestamp": 1768593064.33877, "iteration_found": 18, "metrics": {"combined_score": -90.31099750000001}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 4 lines with 5 lines\nChange 2: Replace 12 lines with 19 lines", "parent_metrics": {"combined_score": -90.31099750000001}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYour task is to improve a routing algorithm that assigns requests to multiple simulator instances.\nThe algorithm receives a list of requests and must decide which simulator each request should be routed to.\nThe goal is to maximize the average reward returned by the simulators.\nGood routing solutions often balance load while preserving request locality (e.g., requests with shared prefix as part of request.input benefiting from reusing same instance routing).\nFocus only on improving the routing logic. Do not change the evaluator or any external code.\n", "user": "# Current Program Information\n- Fitness: -90.3110\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at -90.3110\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### avg_latency\n```\n90.31099750000001\n```\n\n### sim0_requests\n```\n200\n```\n\n### sim1_requests\n```\n200\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 13 lines with 60 lines\n- Metrics: combined_score: -90.3110\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace 9 lines with 56 lines\n- Metrics: combined_score: -90.3110\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Change 1: Replace 4 lines with 7 lines\nChange 2: Replace 5 lines with 6 lines\n- Metrics: combined_score: -90.3110\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -90.3110)\n```python\n# EVOLVE-BLOCK-START\nimport random # Not used in this specific version, but kept for potential future variations.\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version routes requests based on a deterministic hash of a common prefix\n    extracted from request.input. It aims to maximize locality by routing requests\n    with similar prefixes to the same simulator, while also balancing load across simulators.\n\n    The prefix (routing_key) is heuristically defined as:\n    - The request.input string up to the last '/' character, if a '/' is present.\n    - The entire request.input string, if no '/' is found.\n\n    This strategy assumes that the \"shared prefix\" benefit applies to hierarchical\n    or path-like inputs where related requests share a common parent segment.\n    \"\"\"\n    policy = []\n    \n    # This dictionary maps extracted routing keys (prefixes) to simulator IDs.\n    # It ensures that all requests with the same routing_key within this batch\n    # are assigned to the same simulator, preserving locality.\n    prefix_to_sim_map = {} \n    \n    # Initialize a counter for round-robin assignment of new prefixes.\n    next_sim_to_assign_prefix = 0\n\n    for req in requests:\n        # Ensure req.input is treated as a string for consistent hashing.\n        input_data = str(req.input)\n\n        # Determine the routing key based on a common prefix.\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        # Assign a simulator ID to this routing key if it hasn't been seen yet\n        # in the current batch.\n        if routing_key not in prefix_to_sim_map:\n            # Assign prefixes in a round-robin fashion to improve load balancing\n            # for unique prefixes, while still preserving locality for repeated prefixes.\n            assigned_sim_id = next_sim_to_assign_prefix\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n            next_sim_to_assign_prefix = (next_sim_to_assign_prefix + 1) % num_sims\n        \n        # Retrieve the assigned simulator ID for the current request's routing_key.\n        simulator_id = prefix_to_sim_map[routing_key]\n        policy.append(simulator_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-90.3110)\n\n### Program 2 (Score: -90.3110)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version aims to maximize locality by routing requests with shared prefixes\n    to the same simulator, while also balancing the load across simulators\n    by assigning new prefixes to the least loaded simulator at that point in the batch.\n\n    The prefix (routing_key) is extracted from request.input:\n    - The request.input string up to the last '/' character, if present.\n    - The entire request.input string, if no '/' is found.\n    \"\"\"\n    policy = []\n    \n    # Maps extracted routing keys (prefixes) to simulator IDs.\n    # Ensures all requests with the same routing_key go to the same simulator.\n    prefix_to_sim_map = {} \n\n    # Tracks the number of requests assigned to each simulator in the current batch.\n    # Used to make load-aware decisions for new prefixes.\n    sim_loads = [0] * num_sims\n\n    for req in requests:\n        input_data = str(req.input)\n\n        # Determine the routing key (prefix)\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        assigned_sim_id = None\n        if routing_key in prefix_to_sim_map:\n            # If prefix seen before, use its assigned simulator to preserve locality.\n            assigned_sim_id = prefix_to_sim_map[routing_key]\n        else:\n            # If new prefix, assign it to the currently least loaded simulator.\n            # This helps balance load as new prefixes are introduced.\n            least_loaded_sim_id = 0\n            min_load = sim_loads[0]\n            for i in range(1, num_sims):\n                if sim_loads[i] < min_load:\n                    min_load = sim_loads[i]\n                    least_loaded_sim_id = i\n            \n            assigned_sim_id = least_loaded_sim_id\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n        \n        # Update load for the assigned simulator\n        sim_loads[assigned_sim_id] += 1\n        policy.append(assigned_sim_id)\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-90.3110)\n\n### Program 3 (Score: -90.3110)\n```python\n# EVOLVE-BLOCK-START\nimport random # Not used in core routing logic here, but kept for consistency or potential future variations.\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version aims to maximize locality by routing requests with shared prefixes\n    to the same simulator, while also balancing the load across simulators\n    by assigning new prefixes to the least loaded simulator at that point in the batch.\n\n    The prefix (routing_key) is extracted from request.input:\n    - The request.input string up to the last '/' character, if present.\n    - The entire request.input string, if no '/' is found.\n    \"\"\"\n    policy = []\n    \n    # Maps extracted routing keys (prefixes) to simulator IDs.\n    # Ensures all requests with the same routing_key go to the same simulator.\n    prefix_to_sim_map = {} \n\n    # Tracks the number of requests assigned to each simulator in the current batch.\n    # Used to make load-aware decisions for new prefixes.\n    sim_loads = [0] * num_sims\n\n    for req in requests:\n        input_data = str(req.input)\n\n        # Determine the routing key (prefix)\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        assigned_sim_id = None\n        if routing_key in prefix_to_sim_map:\n            # If prefix seen before, use its assigned simulator to preserve locality.\n            assigned_sim_id = prefix_to_sim_map[routing_key]\n        else:\n            # If new prefix, assign it to the currently least loaded simulator.\n            # This helps balance load as new prefixes are introduced.\n            least_loaded_sim_id = 0\n            min_load = sim_loads[0]\n            for i in range(1, num_sims):\n                if sim_loads[i] < min_load:\n                    min_load = sim_loads[i]\n                    least_loaded_sim_id = i\n            \n            assigned_sim_id = least_loaded_sim_id\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n        \n        # Update load for the assigned simulator\n        sim_loads[assigned_sim_id] += 1\n        policy.append(assigned_sim_id)\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-90.3110)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -90.3110)\n```python\n# EVOLVE-BLOCK-START\nimport random\nfrom collections import defaultdict\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n\n    This router aims to maximize the average reward by balancing load across simulators\n    while preserving request locality within each batch. It achieves this by:\n    1. Extracting a 'routing_key' (prefix) from each request.input.\n    2. Grouping requests by these routing keys and counting their occurrences in the batch.\n    3. Assigning each unique routing key to the least loaded simulator in the current batch.\n       This greedy assignment prioritizes optimal load distribution for the current batch.\n    4. All requests sharing the same routing key within this batch are routed to the same simulator,\n       providing locality benefits for co-located requests.\n\n    This approach prioritizes excellent within-batch load balancing and locality over\n    strict cross-batch deterministic locality (i.e., a given prefix might be routed to different\n    simulators in different batches). This strategy is chosen based on the observation that\n    a purely random router (which offers strong within-batch load balancing) outperformed\n    a deterministic-hashing-based router (which offers strong cross-batch locality but\n    can lead to within-batch load imbalance).\n    \"\"\"\n    if not requests:\n        return []\n\n    # Step 1: Group requests by routing key and store their original indices\n    key_request_indices = defaultdict(list)\n    for i, req in enumerate(requests):\n        input_data = str(req.input)\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            routing_key = input_data\n        key_request_indices[routing_key].append(i)\n    \n    # Step 2: Sort routing keys by their total request count (descending)\n    # Assigning larger groups first helps in making better greedy assignments for load balancing.\n    sorted_keys = sorted(key_request_indices.items(), key=lambda item: len(item[1]), reverse=True)\n\n    # Step 3: Assign each routing key to a simulator, prioritizing load balance\n    policy = [0] * len(requests) # Pre-allocate policy list for efficiency\n    current_sim_loads = [0] * num_sims # Tracks the load assigned to each simulator in the current batch\n\n    for routing_key, indices in sorted_keys:\n        # Find the simulator with the minimum current load\n        min_load = float('inf')\n        min_load_sim_id = -1\n        # Iterate through simulators to find the least loaded one.\n        # If there's a tie, the one with the smallest ID is chosen (deterministic tie-breaking).\n        for sim_id in range(num_sims):\n            if current_sim_loads[sim_id] < min_load:\n                min_load = current_sim_loads[sim_id]\n                min_load_sim_id = sim_id\n        \n        # Assign this routing_key to the least loaded simulator for the current batch\n        assigned_sim_id = min_load_sim_id\n        current_sim_loads[assigned_sim_id] += len(indices)\n        \n        # Populate the policy for all requests belonging to this routing_key\n        for original_index in indices:\n            policy[original_index] = assigned_sim_id\n\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to combined_score\n\n### Program D2 (Score: -108.8020)\n```python\n# EVOLVE-BLOCK-START\nimport random # Not used in this specific version, but kept for potential future variations.\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version routes requests based on a deterministic hash of a common prefix\n    extracted from request.input. It aims to maximize locality by routing requests\n    with similar prefixes to the same simulator, while also balancing load across simulators.\n\n    The prefix (routing_key) is heuristically defined as:\n    - The request.input string up to the last '/' character, if a '/' is present.\n    - The entire request.input string, if no '/' is found.\n\n    This strategy assumes that the \"shared prefix\" benefit applies to hierarchical\n    or path-like inputs where related requests share a common parent segment.\n    \"\"\"\n    policy = []\n    \n    # This dictionary maps extracted routing keys (prefixes) to simulator IDs.\n    # It ensures that all requests with the same routing_key within this batch\n    # are assigned to the same simulator, preserving locality.\n    prefix_to_sim_map = {}\n\n    # Initialize a counter to distribute the first assignment of new prefixes\n    # in a round-robin fashion across simulators. This helps improve load\n    # distribution for unique prefixes, especially when `hash()` might lead to bias\n    # for a small number of simulators.\n    current_prefix_assignment_idx = 0\n\n    for req in requests:\n        # Ensure req.input is treated as a string for consistent hashing.\n        input_data = str(req.input)\n\n        # Determine the routing key based on a common prefix.\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        # Assign a simulator ID to this routing key if it hasn't been seen yet\n        # in the current batch.\n        if routing_key not in prefix_to_sim_map:\n            # Assign simulator IDs to new routing keys in a round-robin fashion\n            # to improve load distribution across simulators for unique prefixes.\n            assigned_sim_id = current_prefix_assignment_idx % num_sims\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n            current_prefix_assignment_idx += 1 # Increment for the next unique prefix\n        \n        # Retrieve the assigned simulator ID for the current request's routing_key.\n        simulator_id = prefix_to_sim_map[routing_key]\n        policy.append(simulator_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -90.3110, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random # Not used in core routing logic here, but kept for consistency or potential future variations.\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version aims to maximize locality by routing requests with shared prefixes\n    to the same simulator, while also balancing the load across simulators\n    by assigning new prefixes to the least loaded simulator at that point in the batch.\n\n    The prefix (routing_key) is extracted from request.input:\n    - The request.input string up to the last '/' character, if present.\n    - The entire request.input string, if no '/' is found.\n    \"\"\"\n    policy = []\n    \n    # Maps extracted routing keys (prefixes) to simulator IDs.\n    # Ensures all requests with the same routing_key go to the same simulator.\n    prefix_to_sim_map = {} \n\n    # Tracks the number of requests assigned to each simulator in the current batch.\n    # Used to make load-aware decisions for new prefixes.\n    sim_loads = [0] * num_sims\n\n    for req in requests:\n        input_data = str(req.input)\n\n        # Determine the routing key (prefix)\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        assigned_sim_id = None\n        if routing_key in prefix_to_sim_map:\n            # If prefix seen before, use its assigned simulator to preserve locality.\n            assigned_sim_id = prefix_to_sim_map[routing_key]\n        else:\n            # If new prefix, assign it to the currently least loaded simulator.\n            # This helps balance load as new prefixes are introduced.\n            least_loaded_sim_id = 0\n            min_load = sim_loads[0]\n            for i in range(1, num_sims):\n                if sim_loads[i] < min_load:\n                    min_load = sim_loads[i]\n                    least_loaded_sim_id = i\n            \n            assigned_sim_id = least_loaded_sim_id\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n        \n        # Update load for the assigned simulator\n        sim_loads[assigned_sim_id] += 1\n        policy.append(assigned_sim_id)\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], Mixed iteration strategies\n\n### Inspiration 2 (Score: -90.3110, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version aims to maximize locality by routing requests with shared prefixes\n    to the same simulator, while also balancing the load across simulators\n    by assigning new prefixes to the least loaded simulator at that point in the batch.\n\n    The prefix (routing_key) is extracted from request.input:\n    - The request.input string up to the last '/' character, if present.\n    - The entire request.input string, if no '/' is found.\n    \"\"\"\n    policy = []\n    \n    # Maps extracted routing keys (prefixes) to simulator IDs.\n    # Ensures all requests with the same routing_key go to the same simulator.\n    prefix_to_sim_map = {} \n\n    # Tracks the number of requests assigned to each simulator in the current batch.\n    # Used to make load-aware decisions for new prefixes.\n    sim_loads = [0] * num_sims\n\n    for req in requests:\n        input_data = str(req.input)\n\n        # Determine the routing key (prefix)\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        assigned_sim_id = None\n        if routing_key in prefix_to_sim_map:\n            # If prefix seen before, use its assigned simulator to preserve locality.\n            assigned_sim_id = prefix_to_sim_map[routing_key]\n        else:\n            # If new prefix, assign it to the currently least loaded simulator.\n            # This helps balance load as new prefixes are introduced.\n            least_loaded_sim_id = 0\n            min_load = sim_loads[0]\n            for i in range(1, num_sims):\n                if sim_loads[i] < min_load:\n                    min_load = sim_loads[i]\n                    least_loaded_sim_id = i\n            \n            assigned_sim_id = least_loaded_sim_id\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n        \n        # Update load for the assigned simulator\n        sim_loads[assigned_sim_id] += 1\n        policy.append(assigned_sim_id)\n        \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], Mixed iteration strategies\n\n### Inspiration 3 (Score: -inf, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random # Not used in this specific version, but kept for potential future variations.\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version routes requests based on a deterministic hash of a common prefix\n    extracted from request.input. It aims to maximize locality by routing requests\n    with similar prefixes to the same simulator, while also balancing load across simulators.\n\n    The prefix (routing_key) is heuristically defined as:\n    - The request.input string up to the last '/' character, if a '/' is present.\n    - The entire request.input string, if no '/' is found.\n\n    This strategy assumes that the \"shared prefix\" benefit applies to hierarchical\n    or path-like inputs where related requests share a common parent segment.\n    \"\"\"\n    policy = []\n    \n    # This dictionary maps extracted routing keys (prefixes) to simulator IDs.\n    # It ensures that all requests with the same routing_key within this batch\n    # are assigned to the same simulator, preserving locality.\n    prefix_to_sim_map = {} \n\n    for req in requests:\n        # Ensure req.input is treated as a string for consistent hashing.\n        input_data = str(req.input)\n\n        # Determine the routing key based on a common prefix.\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        # Assign a simulator ID to this routing key if it hasn't been seen yet\n        # in the current batch.\n        if routing_key not in prefix_to_sim_map:\n            # Python's built-in hash() provides a reasonably good distribution for strings.\n            # The modulo operation ensures the simulator ID falls within [0, num_sims - 1].\n            assigned_sim_id = hash(routing_key) % num_sims\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n        \n        # Retrieve the assigned simulator ID for the current request's routing_key.\n        simulator_id = prefix_to_sim_map[routing_key]\n        policy.append(simulator_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], Mixed iteration strategies\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\nimport random # Not used in this specific version, but kept for potential future variations.\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This version routes requests based on a deterministic hash of a common prefix\n    extracted from request.input. It aims to maximize locality by routing requests\n    with similar prefixes to the same simulator, while also balancing load across simulators.\n\n    The prefix (routing_key) is heuristically defined as:\n    - The request.input string up to the last '/' character, if a '/' is present.\n    - The entire request.input string, if no '/' is found.\n\n    This strategy assumes that the \"shared prefix\" benefit applies to hierarchical\n    or path-like inputs where related requests share a common parent segment.\n    \"\"\"\n    policy = []\n    \n    # This dictionary maps extracted routing keys (prefixes) to simulator IDs.\n    # It ensures that all requests with the same routing_key within this batch\n    # are assigned to the same simulator, preserving locality.\n    prefix_to_sim_map = {} \n    \n    # Initialize a counter for round-robin assignment of new prefixes.\n    next_sim_to_assign_prefix = 0\n\n    for req in requests:\n        # Ensure req.input is treated as a string for consistent hashing.\n        input_data = str(req.input)\n\n        # Determine the routing key based on a common prefix.\n        # This approach seeks to group requests that share a 'parent' identifier\n        # (e.g., 'user/123/profile' and 'user/123/orders' would both use 'user/123' as the key).\n        last_slash_idx = input_data.rfind('/')\n        if last_slash_idx != -1:\n            routing_key = input_data[:last_slash_idx]\n        else:\n            # If no slash, the entire input string is considered the key.\n            routing_key = input_data\n\n        # Assign a simulator ID to this routing key if it hasn't been seen yet\n        # in the current batch.\n        if routing_key not in prefix_to_sim_map:\n            # Assign prefixes in a round-robin fashion to improve load balancing\n            # for unique prefixes, while still preserving locality for repeated prefixes.\n            assigned_sim_id = next_sim_to_assign_prefix\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n            next_sim_to_assign_prefix = (next_sim_to_assign_prefix + 1) % num_sims\n        \n        # Retrieve the assigned simulator ID for the current request's routing_key.\n        simulator_id = prefix_to_sim_map[routing_key]\n        policy.append(simulator_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current routing algorithm uses a round-robin strategy for assigning *new* prefixes to simulators. While this provides some basic load balancing for unique prefixes, it doesn't account for the actual number of requests associated with each prefix, which can lead to load imbalances if some prefixes appear much more frequently than others.\n\nThe proposed improvement is to switch from a simple round-robin assignment for new prefixes to a \"least loaded simulator\" approach. This strategy, as seen in other top-performing programs, dynamically assigns new prefixes to the simulator that currently has the fewest requests assigned to it *within the current batch*. This provides a more robust load balancing mechanism, as it considers the cumulative load distributed so far, potentially leading to a more even distribution of requests across simulators. Maintaining within-batch locality for existing prefixes is preserved.\n\nEven though existing top-performing programs already have the same score, this change is a theoretically sound improvement in load balancing logic and can be expected to perform better on a wider variety of request distributions, fulfilling the task of improving the routing logic.\n\nI'll perform two changes:\n1.  Replace the `next_sim_to_assign_prefix` initialization with `sim_loads`.\n2.  Modify the loop logic to use `sim_loads` to find the least loaded simulator for new prefixes and update `sim_loads` after each request assignment.\n\n<<<<<<< SEARCH\n    prefix_to_sim_map = {} \n    \n    # Initialize a counter for round-robin assignment of new prefixes.\n    next_sim_to_assign_prefix = 0\n=======\n    prefix_to_sim_map = {} \n    \n    # Tracks the number of requests assigned to each simulator in the current batch.\n    # Used to make load-aware decisions for new prefixes.\n    sim_loads = [0] * num_sims\n>>>>>>> REPLACE\n\n<<<<<<< SEARCH\n        # Assign a simulator ID to this routing key if it hasn't been seen yet\n        # in the current batch.\n        if routing_key not in prefix_to_sim_map:\n            # Assign prefixes in a round-robin fashion to improve load balancing\n            # for unique prefixes, while still preserving locality for repeated prefixes.\n            assigned_sim_id = next_sim_to_assign_prefix\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n            next_sim_to_assign_prefix = (next_sim_to_assign_prefix + 1) % num_sims\n        \n        # Retrieve the assigned simulator ID for the current request's routing_key.\n        simulator_id = prefix_to_sim_map[routing_key]\n        policy.append(simulator_id)\n=======\n        # Assign a simulator ID to this routing key, preserving locality if seen before.\n        assigned_sim_id = None\n        if routing_key in prefix_to_sim_map:\n            assigned_sim_id = prefix_to_sim_map[routing_key]\n        else:\n            # For new prefixes, assign to the currently least loaded simulator.\n            least_loaded_sim_id = 0\n            min_load = sim_loads[0]\n            for i in range(1, num_sims):\n                if sim_loads[i] < min_load:\n                    min_load = sim_loads[i]\n                    least_loaded_sim_id = i\n            \n            assigned_sim_id = least_loaded_sim_id\n            prefix_to_sim_map[routing_key] = assigned_sim_id\n        \n        # Update load for the assigned simulator and append to policy.\n        sim_loads[assigned_sim_id] += 1\n        policy.append(assigned_sim_id)\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"avg_latency\": 90.31099750000001, \"sim0_requests\": 200, \"sim1_requests\": 200}", "artifact_dir": null, "embedding": null}