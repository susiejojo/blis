{"id": "63bdd49b-d500-43c5-9831-3063743a2e70", "code": "# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    n_requests = len(requests)\n    if n_requests == 0:\n        return []\n\n    policy = [0] * n_requests \n    \n    # Stores preferred simulator ID for each request index\n    # and also a list of indices for each preferred simulator\n    preferred_assignments_per_request_idx = [0] * n_requests\n    preferred_groups_by_sim = [[] for _ in range(num_sims)]\n\n    for i, req in enumerate(requests):\n        # Calculate preferred simulator based on hash for locality\n        preferred_sim_id = hash(req.input) % num_sims\n        preferred_assignments_per_request_idx[i] = preferred_sim_id\n        preferred_groups_by_sim[preferred_sim_id].append(i)\n\n    # Calculate desired average load per simulator (ceiling for fair distribution)\n    base_load_per_sim = n_requests // num_sims\n    \n    # To manage load distribution, we'll keep track of actual assignments made\n    actual_sim_loads = [0] * num_sims\n    \n    # Store indices of requests that need to be reassigned because their\n    # preferred simulator became \"overloaded\" early in the assignment process.\n    reassign_candidates = []\n\n    # First pass: Assign requests greedily to their preferred simulator,\n    # up to a certain threshold (max_allowed_for_locality).\n    # Requests beyond this threshold become candidates for rebalancing.\n    for sim_id in range(num_sims):\n        group = preferred_groups_by_sim[sim_id]\n        \n        # Sort requests within each group by their original index for deterministic behavior\n        group.sort() \n        \n        # Calculate the maximum number of requests this simulator can take from its preferred group\n        # This distributes the remainder (n_requests % num_sims) evenly among the first few simulators\n        max_allowed_for_locality = base_load_per_sim + (1 if sim_id < (n_requests % num_sims) else 0)\n\n        # Assign requests to their preferred sim if capacity allows\n        for i, req_idx in enumerate(group):\n            if actual_sim_loads[sim_id] < max_allowed_for_locality: \n                policy[req_idx] = sim_id\n                actual_sim_loads[sim_id] += 1\n            else: # These requests exceed the locality limit for this sim, mark for rebalancing\n                reassign_candidates.append(req_idx)\n\n    # Step 3: Reassign candidates to the least loaded simulators.\n    # Shuffle the reassign candidates to break any potential patterns from original ordering\n    random.shuffle(reassign_candidates) \n    \n    for req_idx in reassign_candidates:\n        # Find the simulator with the minimum current load\n        least_loaded_sim = actual_sim_loads.index(min(actual_sim_loads))\n        policy[req_idx] = least_loaded_sim\n        actual_sim_loads[least_loaded_sim] += 1\n            \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n", "language": "python", "parent_id": "0397e891-cc7c-4d3a-8a74-8ecd0e23e696", "generation": 2, "timestamp": 1768941119.071721, "iteration_found": 9, "metrics": {"combined_score": -648.1388499999999}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 8 lines with 59 lines", "parent_metrics": {"combined_score": -Infinity}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYour task is to improve a routing algorithm that assigns requests to multiple simulator instances.\nThe algorithm receives a list of requests and must decide which simulator each request should be routed to.\nThe goal is to maximize the average reward returned by the simulators.\nGood routing solutions often balance load while preserving request locality (e.g., requests with shared prefix as part of request.input benefiting from reusing same instance routing).\nFocus only on improving the routing logic. Do not change the evaluator or any external code.\n", "user": "# Current Program Information\n- Fitness: -inf\n- Feature coordinates: \n- Focus areas: - No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### avg_latency\n```\ninf\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 2 lines with 5 lines\n- Metrics: combined_score: -inf\n- Outcome: Regression in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace 2 lines with 5 lines\n- Metrics: combined_score: -inf\n- Outcome: Regression in all metrics\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: combined_score: -670.4293\n- Outcome: Regression in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -670.4293)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-670.4293)\n\n### Program 2 (Score: -inf)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-inf)\n\n### Program 3 (Score: -inf)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-inf)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -inf)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -670.4293, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -inf, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -inf, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current routing algorithm uses `hash(req.input) % num_sims`. While this provides perfect locality for identical request inputs, it's highly susceptible to extreme load imbalance if certain `req.input` values are much more frequent or if the hash distribution is skewed, especially for a small `num_sims`. The current fitness of `-inf` indicates a catastrophic failure, likely due to such an imbalance. In contrast, the random routing policy (Program 1) achieves a finite, albeit negative, score, suggesting that basic load distribution is critical.\n\nThe goal is to balance load while preserving locality. The proposed improvement implements a two-phase routing strategy within a single batch:\n\n1.  **Locality-Preferring Assignment:** Each request initially \"prefers\" a simulator determined by `hash(req.input) % num_sims`. Requests are tentatively assigned to their preferred simulator, but only up to a calculated \"fair share\" limit (`ceil(total_requests / num_sims)`). This preserves locality for a significant portion of requests.\n2.  **Load-Balancing Reassignment:** Any requests that could not be assigned to their preferred simulator (because that simulator reached its \"fair share\" limit) are collected as \"reassignment candidates.\" These candidates are then distributed among all simulators by assigning them, one by one, to the *currently least loaded* simulator. This step ensures that no single simulator is overwhelmingly overloaded, preventing the `-inf` fitness, and spreads the remaining load. The `random.shuffle` on reassignment candidates helps to avoid deterministic patterns that could lead to new imbalances.\n\nThis approach aims to maintain as much locality as possible while ensuring a reasonable load distribution across all simulators, combining the benefits of deterministic routing with robust load balancing.\n\n<<<<<<< SEARCH\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n=======\n    n_requests = len(requests)\n    if n_requests == 0:\n        return []\n\n    policy = [0] * n_requests \n    \n    # Stores preferred simulator ID for each request index\n    # and also a list of indices for each preferred simulator\n    preferred_assignments_per_request_idx = [0] * n_requests\n    preferred_groups_by_sim = [[] for _ in range(num_sims)]\n\n    for i, req in enumerate(requests):\n        # Calculate preferred simulator based on hash for locality\n        preferred_sim_id = hash(req.input) % num_sims\n        preferred_assignments_per_request_idx[i] = preferred_sim_id\n        preferred_groups_by_sim[preferred_sim_id].append(i)\n\n    # Calculate desired average load per simulator (ceiling for fair distribution)\n    base_load_per_sim = n_requests // num_sims\n    \n    # To manage load distribution, we'll keep track of actual assignments made\n    actual_sim_loads = [0] * num_sims\n    \n    # Store indices of requests that need to be reassigned because their\n    # preferred simulator became \"overloaded\" early in the assignment process.\n    reassign_candidates = []\n\n    # First pass: Assign requests greedily to their preferred simulator,\n    # up to a certain threshold (max_allowed_for_locality).\n    # Requests beyond this threshold become candidates for rebalancing.\n    for sim_id in range(num_sims):\n        group = preferred_groups_by_sim[sim_id]\n        \n        # Sort requests within each group by their original index for deterministic behavior\n        group.sort() \n        \n        # Calculate the maximum number of requests this simulator can take from its preferred group\n        # This distributes the remainder (n_requests % num_sims) evenly among the first few simulators\n        max_allowed_for_locality = base_load_per_sim + (1 if sim_id < (n_requests % num_sims) else 0)\n\n        # Assign requests to their preferred sim if capacity allows\n        for i, req_idx in enumerate(group):\n            if actual_sim_loads[sim_id] < max_allowed_for_locality: \n                policy[req_idx] = sim_id\n                actual_sim_loads[sim_id] += 1\n            else: # These requests exceed the locality limit for this sim, mark for rebalancing\n                reassign_candidates.append(req_idx)\n\n    # Step 3: Reassign candidates to the least loaded simulators.\n    # Shuffle the reassign candidates to break any potential patterns from original ordering\n    random.shuffle(reassign_candidates) \n    \n    for req_idx in reassign_candidates:\n        # Find the simulator with the minimum current load\n        least_loaded_sim = actual_sim_loads.index(min(actual_sim_loads))\n        policy[req_idx] = least_loaded_sim\n        actual_sim_loads[least_loaded_sim] += 1\n            \n    return policy\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"avg_latency\": 648.1388499999999}", "artifact_dir": null, "embedding": null}