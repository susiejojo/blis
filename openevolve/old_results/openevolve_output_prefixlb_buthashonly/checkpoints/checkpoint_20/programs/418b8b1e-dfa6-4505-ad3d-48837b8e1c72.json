{"id": "418b8b1e-dfa6-4505-ad3d-48837b8e1c72", "code": "# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n", "language": "python", "parent_id": "1f50dc13-ed01-4e05-ba1a-af74c3ee5436", "generation": 3, "timestamp": 1768941161.719845, "iteration_found": 14, "metrics": {"combined_score": -652.92175}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 30 lines with 12 lines", "parent_metrics": {"combined_score": -739.34889}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYour task is to improve a routing algorithm that assigns requests to multiple simulator instances.\nThe algorithm receives a list of requests and must decide which simulator each request should be routed to.\nThe goal is to maximize the average reward returned by the simulators.\nGood routing solutions often balance load while preserving request locality (e.g., requests with shared prefix as part of request.input benefiting from reusing same instance routing).\nFocus only on improving the routing logic. Do not change the evaluator or any external code.\n", "user": "# Current Program Information\n- Fitness: -739.3489\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at -739.3489\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### avg_latency\n```\n739.34889\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 7 lines with 17 lines\n- Metrics: combined_score: -739.3489\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: combined_score: -670.4293\n- Outcome: Regression in all metrics\n\n### Attempt 1\n- Changes: Change 1: Replace 2 lines with 5 lines\n- Metrics: combined_score: -652.7698\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -652.7698)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-652.7698)\n\n### Program 2 (Score: -670.4293)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-670.4293)\n\n### Program 3 (Score: -739.3489)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    # Dictionary to store the assigned simulator ID for each unique request input\n    input_to_sim = {}\n    # Counter for round-robin assignment of new unique inputs\n    current_sim_idx = 0\n\n    for req in requests:\n        # Implement a sticky routing policy with round-robin distribution for unique inputs.\n        # This preserves locality (same input -> same sim) and improves load balancing\n        # by distributing unique inputs evenly across simulators.\n        if req.input in input_to_sim:\n            sim_id = input_to_sim[req.input]\n        else:\n            sim_id = current_sim_idx\n            input_to_sim[req.input] = sim_id\n            current_sim_idx = (current_sim_idx + 1) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-739.3489)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -739.3489)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\n# Global state for persistent mapping across router calls\n# This maps unique request.input values to their assigned simulator IDs.\n_persistent_input_to_sim_map = {}\n# Global counter to distribute initial assignments fairly using round-robin.\n_current_sim_round_robin_idx = 0\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This routing policy aims to balance load while preserving request locality.\n    \"\"\"\n    global _persistent_input_to_sim_map\n    global _current_sim_round_robin_idx\n\n    policy = []\n    for req in requests:\n        sim_id = None\n        # Check if this request input has been seen before\n        if req.input in _persistent_input_to_sim_map:\n            # If yes, route to the same simulator for locality\n            sim_id = _persistent_input_to_sim_map[req.input]\n        else:\n            # If no, assign using round-robin for balanced initial distribution\n            sim_id = _current_sim_round_robin_idx\n            _current_sim_round_robin_idx = (_current_sim_round_robin_idx + 1) % num_sims\n            # Store this assignment for future requests with the same input\n            _persistent_input_to_sim_map[req.input] = sim_id\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to combined_score\n\n### Program D2 (Score: -inf)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -670.4293, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -inf, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -739.3489, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    # Dictionary to store the assigned simulator ID for each unique request input\n    input_to_sim = {}\n    # Counter for round-robin assignment of new unique inputs\n    current_sim_idx = 0\n\n    for req in requests:\n        # Implement a sticky routing policy with round-robin distribution for unique inputs.\n        # This preserves locality (same input -> same sim) and improves load balancing\n        # by distributing unique inputs evenly across simulators.\n        if req.input in input_to_sim:\n            sim_id = input_to_sim[req.input]\n        else:\n            sim_id = current_sim_idx\n            input_to_sim[req.input] = sim_id\n            current_sim_idx = (current_sim_idx + 1) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\nimport random\n\n# Global state for persistent mapping across router calls\n# This maps unique request.input values to their assigned simulator IDs.\n_persistent_input_to_sim_map = {}\n# Global counter to distribute initial assignments fairly using round-robin.\n_current_sim_round_robin_idx = 0\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This routing policy aims to balance load while preserving request locality.\n    \"\"\"\n    global _persistent_input_to_sim_map\n    global _current_sim_round_robin_idx\n\n    policy = []\n    for req in requests:\n        sim_id = None\n        # Check if this request input has been seen before\n        if req.input in _persistent_input_to_sim_map:\n            # If yes, route to the same simulator for locality\n            sim_id = _persistent_input_to_sim_map[req.input]\n        else:\n            # If no, assign using round-robin for balanced initial distribution\n            sim_id = _current_sim_round_robin_idx\n            _current_sim_round_robin_idx = (_current_sim_round_robin_idx + 1) % num_sims\n            # Store this assignment for future requests with the same input\n            _persistent_input_to_sim_map[req.input] = sim_id\n\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program uses a sticky routing policy based on global state (`_persistent_input_to_sim_map` and `_current_sim_round_robin_idx`) to assign unique request inputs to simulators in a round-robin fashion. While this aims for locality and initial load balancing, its performance (-739.3489) is significantly worse than Program 1 (-652.7698).\n\nProgram 1 uses a simpler, deterministic hashing approach: `sim_id = hash(req.input) % num_sims`. This method inherently provides excellent locality (same input always goes to the same simulator) and also tends to distribute requests across simulators effectively, assuming `req.input` values are diverse and the hash function provides good dispersion. This strategy often outperforms explicit round-robin for initial assignments because it handles the long-term distribution of all requests (new and repeat) more robustly, especially if certain inputs become \"hot\".\n\nFurthermore, the current program's use of global state and its length (contributing to the \"code length exceeds 500 characters\" concern) make it more complex than necessary. Adopting the hashing strategy from Program 1 will simplify the code and is expected to improve the fitness score considerably.\n\nTherefore, the proposed improvement is to replace the current sticky routing logic with the more effective and simpler hashing-based routing from Program 1, and remove the unused global state variables.\n\n<<<<<<< SEARCH\n# Global state for persistent mapping across router calls\n# This maps unique request.input values to their assigned simulator IDs.\n_persistent_input_to_sim_map = {}\n# Global counter to distribute initial assignments fairly using round-robin.\n_current_sim_round_robin_idx = 0\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    This routing policy aims to balance load while preserving request locality.\n    \"\"\"\n    global _persistent_input_to_sim_map\n    global _current_sim_round_robin_idx\n\n    policy = []\n    for req in requests:\n        sim_id = None\n        # Check if this request input has been seen before\n        if req.input in _persistent_input_to_sim_map:\n            # If yes, route to the same simulator for locality\n            sim_id = _persistent_input_to_sim_map[req.input]\n        else:\n            # If no, assign using round-robin for balanced initial distribution\n            sim_id = _current_sim_round_robin_idx\n            _current_sim_round_robin_idx = (_current_sim_round_robin_idx + 1) % num_sims\n            # Store this assignment for future requests with the same input\n            _persistent_input_to_sim_map[req.input] = sim_id\n\n        policy.append(sim_id)\n    return policy\n=======\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"avg_latency\": 652.92175}", "artifact_dir": null, "embedding": null}