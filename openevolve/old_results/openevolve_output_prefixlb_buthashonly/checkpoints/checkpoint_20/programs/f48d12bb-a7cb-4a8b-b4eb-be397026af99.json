{"id": "f48d12bb-a7cb-4a8b-b4eb-be397026af99", "code": "# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    n_requests = len(requests)\n    if n_requests == 0:\n        return []\n\n    policy = [0] * n_requests \n    \n    # Stores preferred simulator ID for each request index\n    # and also a list of indices for each preferred simulator\n    preferred_assignments_per_request_idx = [0] * n_requests\n    preferred_groups_by_sim = [[] for _ in range(num_sims)]\n\n    for i, req in enumerate(requests):\n        # Calculate preferred simulator based on hash for locality\n        preferred_sim_id = hash(req.input) % num_sims\n        preferred_assignments_per_request_idx[i] = preferred_sim_id\n        preferred_groups_by_sim[preferred_sim_id].append(i)\n\n    # Calculate desired average load per simulator (ceiling for fair distribution)\n    base_load_per_sim = n_requests // num_sims\n    \n    # To manage load distribution, we'll keep track of actual assignments made\n    actual_sim_loads = [0] * num_sims\n    \n    # Store indices of requests that need to be reassigned because their\n    # preferred simulator became \"overloaded\" early in the assignment process.\n    reassign_candidates = []\n\n    # First pass: Assign requests greedily to their preferred simulator,\n    # up to a certain threshold (max_allowed_for_locality).\n    # Requests beyond this threshold become candidates for rebalancing.\n    for sim_id in range(num_sims):\n        group = preferred_groups_by_sim[sim_id]\n        \n        # Sort requests within each group by their original index for deterministic behavior\n        group.sort() \n        \n        # Calculate the maximum number of requests this simulator can take from its preferred group.\n        # This gives each simulator a small buffer (+1) beyond its average share to favor locality.\n        max_allowed_for_locality = base_load_per_sim + 1\n\n        # Assign requests to their preferred sim if capacity allows\n        for i, req_idx in enumerate(group):\n            if actual_sim_loads[sim_id] < max_allowed_for_locality: \n                policy[req_idx] = sim_id\n                actual_sim_loads[sim_id] += 1\n            else: # These requests exceed the locality limit for this sim, mark for rebalancing\n                reassign_candidates.append(req_idx)\n\n    # Step 3: Reassign candidates to the least loaded simulators.\n    # Shuffle the reassign candidates to break any potential patterns from original ordering\n    random.shuffle(reassign_candidates) \n    \n    for req_idx in reassign_candidates:\n        # Find the simulator with the minimum current load\n        least_loaded_sim = actual_sim_loads.index(min(actual_sim_loads))\n        policy[req_idx] = least_loaded_sim\n        actual_sim_loads[least_loaded_sim] += 1\n            \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n", "language": "python", "parent_id": "63bdd49b-d500-43c5-9831-3063743a2e70", "generation": 3, "timestamp": 1768941201.3920908, "iteration_found": 17, "metrics": {"combined_score": -652.76983}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 3 lines with 3 lines", "parent_metrics": {"combined_score": -648.1388499999999}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYour task is to improve a routing algorithm that assigns requests to multiple simulator instances.\nThe algorithm receives a list of requests and must decide which simulator each request should be routed to.\nThe goal is to maximize the average reward returned by the simulators.\nGood routing solutions often balance load while preserving request locality (e.g., requests with shared prefix as part of request.input benefiting from reusing same instance routing).\nFocus only on improving the routing logic. Do not change the evaluator or any external code.\n", "user": "# Current Program Information\n- Fitness: -648.1388\n- Feature coordinates: \n- Focus areas: - Fitness improved: -652.9217 \u2192 -648.1388\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### avg_latency\n```\n648.1388499999999\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 2 lines with 5 lines\n- Metrics: combined_score: -652.9217\n- Outcome: Improvement in all metrics\n\n### Attempt 2\n- Changes: Change 1: Replace 2 lines with 5 lines\n- Metrics: combined_score: -652.7698\n- Outcome: Improvement in all metrics\n\n### Attempt 1\n- Changes: Change 1: Replace 8 lines with 59 lines\n- Metrics: combined_score: -648.1388\n- Outcome: Improvement in all metrics\n\n## Top Performing Programs\n\n### Program 1 (Score: -648.1388)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    n_requests = len(requests)\n    if n_requests == 0:\n        return []\n\n    policy = [0] * n_requests \n    \n    # Stores preferred simulator ID for each request index\n    # and also a list of indices for each preferred simulator\n    preferred_assignments_per_request_idx = [0] * n_requests\n    preferred_groups_by_sim = [[] for _ in range(num_sims)]\n\n    for i, req in enumerate(requests):\n        # Calculate preferred simulator based on hash for locality\n        preferred_sim_id = hash(req.input) % num_sims\n        preferred_assignments_per_request_idx[i] = preferred_sim_id\n        preferred_groups_by_sim[preferred_sim_id].append(i)\n\n    # Calculate desired average load per simulator (ceiling for fair distribution)\n    base_load_per_sim = n_requests // num_sims\n    \n    # To manage load distribution, we'll keep track of actual assignments made\n    actual_sim_loads = [0] * num_sims\n    \n    # Store indices of requests that need to be reassigned because their\n    # preferred simulator became \"overloaded\" early in the assignment process.\n    reassign_candidates = []\n\n    # First pass: Assign requests greedily to their preferred simulator,\n    # up to a certain threshold (max_allowed_for_locality).\n    # Requests beyond this threshold become candidates for rebalancing.\n    for sim_id in range(num_sims):\n        group = preferred_groups_by_sim[sim_id]\n        \n        # Sort requests within each group by their original index for deterministic behavior\n        group.sort() \n        \n        # Calculate the maximum number of requests this simulator can take from its preferred group\n        # This distributes the remainder (n_requests % num_sims) evenly among the first few simulators\n        max_allowed_for_locality = base_load_per_sim + (1 if sim_id < (n_requests % num_sims) else 0)\n\n        # Assign requests to their preferred sim if capacity allows\n        for i, req_idx in enumerate(group):\n            if actual_sim_loads[sim_id] < max_allowed_for_locality: \n                policy[req_idx] = sim_id\n                actual_sim_loads[sim_id] += 1\n            else: # These requests exceed the locality limit for this sim, mark for rebalancing\n                reassign_candidates.append(req_idx)\n\n    # Step 3: Reassign candidates to the least loaded simulators.\n    # Shuffle the reassign candidates to break any potential patterns from original ordering\n    random.shuffle(reassign_candidates) \n    \n    for req_idx in reassign_candidates:\n        # Find the simulator with the minimum current load\n        least_loaded_sim = actual_sim_loads.index(min(actual_sim_loads))\n        policy[req_idx] = least_loaded_sim\n        actual_sim_loads[least_loaded_sim] += 1\n            \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-648.1388)\n\n### Program 2 (Score: -652.7698)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-652.7698)\n\n### Program 3 (Score: -652.9217)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Performs well on combined_score (-652.9217)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -652.9217)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to combined_score\n\n### Program D2 (Score: -670.4293)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # initial dumb policy (random)\n        policy.append(random.randint(0, num_sims - 1))\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nKey features: Alternative approach to combined_score\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -inf, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -652.7698, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -652.9217, Type: Exploratory)\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    policy = []\n    for req in requests:\n        # Route based on request input for locality and deterministic assignment.\n        # Assuming request.input is hashable (e.g., string, tuple, number).\n        # Requests with the same input will always go to the same simulator.\n        sim_id = hash(req.input) % num_sims\n        policy.append(sim_id)\n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n# EVOLVE-BLOCK-START\nimport random\n\ndef router(requests, num_sims=2):\n    \"\"\"\n    Returns a list of simulator IDs, one per request.\n    \"\"\"\n    n_requests = len(requests)\n    if n_requests == 0:\n        return []\n\n    policy = [0] * n_requests \n    \n    # Stores preferred simulator ID for each request index\n    # and also a list of indices for each preferred simulator\n    preferred_assignments_per_request_idx = [0] * n_requests\n    preferred_groups_by_sim = [[] for _ in range(num_sims)]\n\n    for i, req in enumerate(requests):\n        # Calculate preferred simulator based on hash for locality\n        preferred_sim_id = hash(req.input) % num_sims\n        preferred_assignments_per_request_idx[i] = preferred_sim_id\n        preferred_groups_by_sim[preferred_sim_id].append(i)\n\n    # Calculate desired average load per simulator (ceiling for fair distribution)\n    base_load_per_sim = n_requests // num_sims\n    \n    # To manage load distribution, we'll keep track of actual assignments made\n    actual_sim_loads = [0] * num_sims\n    \n    # Store indices of requests that need to be reassigned because their\n    # preferred simulator became \"overloaded\" early in the assignment process.\n    reassign_candidates = []\n\n    # First pass: Assign requests greedily to their preferred simulator,\n    # up to a certain threshold (max_allowed_for_locality).\n    # Requests beyond this threshold become candidates for rebalancing.\n    for sim_id in range(num_sims):\n        group = preferred_groups_by_sim[sim_id]\n        \n        # Sort requests within each group by their original index for deterministic behavior\n        group.sort() \n        \n        # Calculate the maximum number of requests this simulator can take from its preferred group\n        # This distributes the remainder (n_requests % num_sims) evenly among the first few simulators\n        max_allowed_for_locality = base_load_per_sim + (1 if sim_id < (n_requests % num_sims) else 0)\n\n        # Assign requests to their preferred sim if capacity allows\n        for i, req_idx in enumerate(group):\n            if actual_sim_loads[sim_id] < max_allowed_for_locality: \n                policy[req_idx] = sim_id\n                actual_sim_loads[sim_id] += 1\n            else: # These requests exceed the locality limit for this sim, mark for rebalancing\n                reassign_candidates.append(req_idx)\n\n    # Step 3: Reassign candidates to the least loaded simulators.\n    # Shuffle the reassign candidates to break any potential patterns from original ordering\n    random.shuffle(reassign_candidates) \n    \n    for req_idx in reassign_candidates:\n        # Find the simulator with the minimum current load\n        least_loaded_sim = actual_sim_loads.index(min(actual_sim_loads))\n        policy[req_idx] = least_loaded_sim\n        actual_sim_loads[least_loaded_sim] += 1\n            \n    return policy\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return router\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current algorithm balances load by strictly limiting the number of requests a simulator can take from its preferred group in the first pass (`max_allowed_for_locality`). This limit is calculated to achieve an extremely even initial distribution of \"slots\" for preferred assignments. Any requests exceeding this limit for their preferred simulator are then rebalanced to the least loaded simulator, potentially sacrificing locality.\n\nTo maximize average reward, it's often beneficial to prioritize locality slightly more, as requests with shared characteristics benefit from being on the same simulator. The current strict `max_allowed_for_locality` calculation can be too aggressive in forcing load balance early, potentially pushing requests with high locality benefit into the rebalancing pool.\n\nThe proposed change aims to relax this initial constraint slightly. Instead of `base_load_per_sim + (1 if sim_id < (n_requests % num_sims) else 0)`, which distributes the remainder (`n_requests % num_sims`) among the first few simulators, we can give a small, uniform buffer (e.g., +1) to *all* simulators for their preferred assignments. This means every simulator is allowed to take `base_load_per_sim + 1` requests from its preferred group during the first pass.\n\n**Reasoning for the change:**\n1.  **Increased Locality Preservation:** By allowing each simulator to accept one extra request beyond the strict average (if available from its preferred group), fewer requests will be marked as `reassign_candidates`. This means more requests will maintain their preferred (locality-benefiting) assignment.\n2.  **Trade-off for Reward:** The assumption is that the reward gained from preserving locality for more requests will, on average, outweigh the potential slight initial load imbalance. The subsequent rebalancing step (`Step 3`) will still ensure a reasonably balanced final distribution by assigning `reassign_candidates` to the least loaded simulators.\n3.  **Simplicity:** The change simplifies the `max_allowed_for_locality` calculation by removing a conditional expression, which aligns with the \"simplify code length\" hint. Since `n_requests` is guaranteed to be greater than 0 at this point (due to the `if n_requests == 0:` guard), `base_load_per_sim + 1` is safe.\n\nThis modification explores a slightly different point in the locality-vs-load-balance trade-off space, prioritizing locality more in the initial greedy assignment phase, which should lead to higher average rewards if locality benefits are significant.\n\n<<<<<<< SEARCH\n        # Calculate the maximum number of requests this simulator can take from its preferred group\n        # This distributes the remainder (n_requests % num_sims) evenly among the first few simulators\n        max_allowed_for_locality = base_load_per_sim + (1 if sim_id < (n_requests % num_sims) else 0)\n=======\n        # Calculate the maximum number of requests this simulator can take from its preferred group.\n        # This gives each simulator a small buffer (+1) beyond its average share to favor locality.\n        max_allowed_for_locality = base_load_per_sim + 1\n>>>>>>> REPLACE"]}}, "artifacts_json": "{\"avg_latency\": 652.76983}", "artifact_dir": null, "embedding": null}