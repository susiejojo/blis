{"id": "b65b016f-8545-4eb5-b3fb-337f53545f9c", "code": "\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie-breaking\n        best_pods = []\n        best_score = -1\n        \n        # Track request counts per pod for load balancing\n        if not hasattr(prefix_aware_router, 'pod_counts'):\n            prefix_aware_router.pod_counts = [0] * num_sims\n        \n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Weight earlier blocks more heavily (exponential decay)\n                    score += (1.0 / (1 + idx * 0.1))\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n            elif score > 0 and score >= best_score - 0.5:\n                # Consider pods with slightly lower scores if they have less cache pressure\n                if len(pod_cache) < len(POD_KV_CACHE.get(best_pods[0], set())):\n                    best_pods.append(pod_id)\n        \n        # Tie-breaking: weighted selection based on load\n        if len(best_pods) > 1:\n            # Calculate weights inversely proportional to load\n            weights = [1.0 / (1 + prefix_aware_router.pod_counts[p]) for p in best_pods]\n            total_weight = sum(weights)\n            \n            # Deterministic selection based on request index for reproducibility\n            selection_value = (len(policy) * 0.618033988749895) % 1  # Golden ratio for good distribution\n            cumulative = 0\n            for i, pod in enumerate(best_pods):\n                cumulative += weights[i] / total_weight\n                if selection_value < cumulative:\n                    best_pod = pod\n                    break\n            else:\n                best_pod = best_pods[-1]\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n        prefix_aware_router.pod_counts[best_pod] += 1\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys) # DONT CHANGE HERE!!!\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router", "language": "python", "parent_id": "be1f96be-12e3-4352-b9a9-8bfe98c339ff", "generation": 2, "timestamp": 1770394089.454318, "iteration_found": 8, "metrics": {"combined_score": -20816.997607706096}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace 7 lines with 8 lines\nChange 2: Replace # ================== with 12 lines\nChange 3: Replace 5 lines with 18 lines\nChange 4: Replace 5 lines with 9 lines", "parent_metrics": {"combined_score": -20816.997607706096}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert systems programmer.\nYou are optimizing a Python request router for large language model inference.\nThe router assigns each request to one of N pods by:\nTokenizing the prompt\nHashing tokens into KV blocks\nScoring pods by longest prefix match with their KV cache\nRouting to the pod with the highest score\nUpdating that pods KV cache after routing If no prefix match exists, a round-robin fallback is used.\nThe router is evaluated in a simulator that measures end-to-end latency (average and tail).\nGoal: The goal is to maximize the average reward returned by the simulators. You May\nModify the prefix scoring logic\nChange tie-breaking behavior\nAdd lightweight load-aware or probabilistic decisions\nIntroduce simple thresholds or heuristics\n", "user": "# Current Program Information\n- Fitness: -20816.9976\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at -20816.9976\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### avg_latency\n```\n20816.997607706096\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace 14 lines with 27 lines\nChange 2: Replace # ================== with 12 lines\nChange 3: Replace 5 lines with 6 lines\n- Metrics: combined_score: -20816.9976\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: combined_score: -20816.9976\n- Outcome: Regression in all metrics\n\n### Attempt 1\n- Changes: Change 1: Replace 14 lines with 29 lines\nChange 2: Replace # ================== with 15 lines\n- Metrics: combined_score: -20816.9976\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -20816.9976)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load-aware tie-breaking\n        best_pods = []\n        best_score = -1\n        pod_loads = [0] * num_sims\n        \n        # Count current assignments to estimate load\n        for p in policy:\n            pod_loads[p] += 1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Load-aware selection among tied pods\n        if len(best_pods) > 1:\n            # Pick the least loaded pod among ties\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys) # DONT CHANGE HERE!!!\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-20816.9976)\n\n### Program 2 (Score: -20816.9976)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys) # DONT CHANGE HERE!!!\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-20816.9976)\n\n### Program 3 (Score: -20816.9976)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie-breaking\n        best_pods = []\n        best_score = -1\n        \n        # Track request counts per pod for load balancing\n        if not hasattr(prefix_aware_router, 'pod_counts'):\n            prefix_aware_router.pod_counts = [0] * num_sims\n        \n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Tie-breaking: choose pod with lowest request count\n        if len(best_pods) > 1:\n            best_pod = min(best_pods, key=lambda p: prefix_aware_router.pod_counts[p])\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n        prefix_aware_router.pod_counts[best_pod] += 1\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys) # DONT CHANGE HERE!!!\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nKey features: Performs well on combined_score (-20816.9976)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -20816.9976, Type: Exploratory)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with load-aware tie-breaking\n        best_pods = []\n        best_score = -1\n        pod_loads = [0] * num_sims\n        \n        # Count current assignments to estimate load\n        for p in policy:\n            pod_loads[p] += 1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Load-aware selection among tied pods\n        if len(best_pods) > 1:\n            # Pick the least loaded pod among ties\n            best_pod = min(best_pods, key=lambda p: pod_loads[p])\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys) # DONT CHANGE HERE!!!\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], Comprehensive implementation\n\n### Inspiration 2 (Score: -20816.9976, Type: Exploratory)\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring (inline)\n        best_pod = 0\n        best_score = -1\n\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            if score > best_score:\n                best_score = score\n                best_pod = pod_id\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys) # DONT CHANGE HERE!!!\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\nUnique approach: [Fragment formatting error: 'metric_name'], Comprehensive implementation\n\n# Current Program\n```python\n\"\"\"\nPrefix-aware router (LLMD-style).\n\n- Global pod KV cache\n- Deterministic KV block hashing (struct.pack)\n- Longest-prefix scoring\n- Offline, sequential routing (OpenEvolve / BLIS friendly)\n\"\"\"\n\nimport hashlib\nimport struct\nfrom typing import List, Dict, Any\n\nfrom transformers import AutoTokenizer\n\n\n# =========================\n# Global KV cache per pod\n# =========================\n\nPOD_KV_CACHE: Dict[int, set] = {}\n\n\ndef tokenize_prompt(prompt: str, tokenizer) -> List[int]:\n    \"\"\"Tokenize prompt into token IDs.\"\"\"\n    return tokenizer.encode(prompt, add_special_tokens=False)\n\n\ndef tokens_to_kv_block_keys(\n    tokens: List[int],\n    kv_block_size: int = 16,\n) -> List[str]:\n    \"\"\"Convert token IDs into KV block hashes (uint32 packed).\"\"\"\n    block_keys = []\n\n    for i in range(0, len(tokens), kv_block_size):\n        chunk = tokens[i : i + kv_block_size]\n        h = hashlib.sha256()\n        for t in chunk:\n            h.update(struct.pack(\"I\", t))  # uint32\n        block_keys.append(h.hexdigest())\n\n    return block_keys\n\n# EVOLVE-BLOCK-START\ndef prefix_aware_router(\n    requests: List[Any],\n    num_sims: int,\n    model_name: str = \"codellama/CodeLlama-34b-Instruct-hf\", # no need to change this - we use default\n    kv_block_size: int = 16, # no need to change this - we use default\n) -> List[int]:\n    \"\"\"\n    Prefix-aware router.\n\n    Args:\n        requests: list of objects with `.input`\n        num_sims: number of pods / simulators\n\n    Returns:\n        policy: chosen pod per request\n    \"\"\"\n    global POD_KV_CACHE\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n\n    # Initialize pod KV cache\n    if not POD_KV_CACHE or len(POD_KV_CACHE) != num_sims:\n        POD_KV_CACHE = {pod_id: set() for pod_id in range(num_sims)}\n\n    policy = []\n\n    for req in requests:\n        # 1. Tokenize\n        tokens = tokenize_prompt(req.input, tokenizer)\n\n        # 2. KV block hashes\n        block_keys = tokens_to_kv_block_keys(tokens, kv_block_size)\n\n        # 3. Longest-prefix scoring with tie-breaking\n        best_pods = []\n        best_score = -1\n        \n        # Track request counts per pod for load balancing\n        if not hasattr(prefix_aware_router, 'pod_counts'):\n            prefix_aware_router.pod_counts = [0] * num_sims\n        \n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n            \n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n        \n        # Tie-breaking: choose pod with lowest request count\n        if len(best_pods) > 1:\n            best_pod = min(best_pods, key=lambda p: prefix_aware_router.pod_counts[p])\n        else:\n            best_pod = best_pods[0]\n\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n\n        # 4. Route\n        policy.append(best_pod)\n        prefix_aware_router.pod_counts[best_pod] += 1\n\n        # 5. Update KV cache AFTER routing\n        POD_KV_CACHE[best_pod].update(block_keys) # DONT CHANGE HERE!!!\n\n    return policy\n\n# EVOLVE-BLOCK-END\n\ndef run_search():\n    # required entry point for evaluator\n    return prefix_aware_router\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["Looking at the current program and its performance history, I notice that the fitness score has been stuck at -20816.9976 across multiple attempts. The main areas for improvement appear to be:\n\n1. The load balancing mechanism using `pod_counts` is good but could be enhanced\n2. The prefix scoring could benefit from weighted scoring based on match position\n3. The cold-start round-robin could be improved with better initial distribution\n\nLet me suggest targeted improvements:\n\n## Improvement 1: Enhanced Prefix Scoring with Position Weighting\n\nThe current implementation treats all prefix matches equally. However, earlier matches (closer to the beginning) are typically more valuable for cache reuse. Let's weight them higher:\n\n<<<<<<< SEARCH\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for key in block_keys:\n                if key in pod_cache:\n                    score += 1\n                else:\n                    break\n=======\n        for pod_id, pod_cache in POD_KV_CACHE.items():\n            score = 0\n            for idx, key in enumerate(block_keys):\n                if key in pod_cache:\n                    # Weight earlier blocks more heavily (exponential decay)\n                    score += (1.0 / (1 + idx * 0.1))\n                else:\n                    break\n>>>>>>> REPLACE\n\n## Improvement 2: Smarter Cold-Start Distribution\n\nInstead of simple round-robin for cold starts, let's distribute based on current load to maintain better balance:\n\n<<<<<<< SEARCH\n        # =========================\n        # Cold-start fairness (Option 1)\n        # =========================\n        if best_score == 0:\n            # round-robin based on request index\n            best_pod = len(policy) % num_sims\n=======\n        # =========================\n        # Load-aware cold-start distribution\n        # =========================\n        if best_score == 0:\n            # Choose least loaded pod for cold starts\n            best_pod = min(range(num_sims), key=lambda p: prefix_aware_router.pod_counts[p])\n>>>>>>> REPLACE\n\n## Improvement 3: Probabilistic Tie-Breaking for Better Distribution\n\nWhen multiple pods have the same score, instead of always choosing the least loaded, we can add some randomness to avoid patterns that might hurt cache locality:\n\n<<<<<<< SEARCH\n        # Tie-breaking: choose pod with lowest request count\n        if len(best_pods) > 1:\n            best_pod = min(best_pods, key=lambda p: prefix_aware_router.pod_counts[p])\n        else:\n            best_pod = best_pods[0]\n=======\n        # Tie-breaking: weighted selection based on load\n        if len(best_pods) > 1:\n            # Calculate weights inversely proportional to load\n            weights = [1.0 / (1 + prefix_aware_router.pod_counts[p]) for p in best_pods]\n            total_weight = sum(weights)\n            \n            # Deterministic selection based on request index for reproducibility\n            selection_value = (len(policy) * 0.618033988749895) % 1  # Golden ratio for good distribution\n            cumulative = 0\n            for i, pod in enumerate(best_pods):\n                cumulative += weights[i] / total_weight\n                if selection_value < cumulative:\n                    best_pod = pod\n                    break\n            else:\n                best_pod = best_pods[-1]\n        else:\n            best_pod = best_pods[0]\n>>>>>>> REPLACE\n\n## Improvement 4: Cache Size Consideration\n\nLet's add a simple mechanism to prefer pods with more available cache space when scores are close:\n\n<<<<<<< SEARCH\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n=======\n            if score > best_score:\n                best_score = score\n                best_pods = [pod_id]\n            elif score == best_score:\n                best_pods.append(pod_id)\n            elif score > 0 and score >= best_score - 0.5:\n                # Consider pods with slightly lower scores if they have less cache pressure\n                if len(pod_cache) < len(POD_KV_CACHE.get(best_pods[0], set())):\n                    best_pods.append(pod_id)\n>>>>>>> REPLACE\n\n## Reasoning\n\nThese improvements address several key aspects:\n\n1. **Position-weighted scoring**: Early cache hits are more valuable because they indicate longer shared prefixes, which typically lead to better cache utilization.\n\n2. **Load-aware cold-start**: Instead of round-robin, we actively balance load from the start, preventing some pods from being overloaded early.\n\n3. **Probabilistic tie-breaking**: Using weighted selection with the golden ratio provides good distribution while maintaining determinism (important for reproducibility).\n\n4. **Cache pressure consideration**: By considering pods with slightly lower scores but less cache pressure, we can achieve better overall system utilization.\n\nThese changes maintain the core algorithm's efficiency while adding lightweight optimizations that should improve latency metrics without adding significant complexity."]}}, "artifacts_json": "{\"avg_latency\": 20816.997607706096}", "artifact_dir": null, "embedding": null}